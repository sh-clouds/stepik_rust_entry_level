Противоположные знаки  🪤🪤

Напишите программу, которая считывает два целых числа a (i8) и b (i8), проверяет знаки данных целых чисел без сравнения самих чисел и выводит результат:

Если числа имеют разные знаки,  вывести сообщение: числа {a} и {b} имеют разные знаки.
Иначе вывести: числа {a} и {b} имеют одинаковые знаки.
Тестовые данные ✅

Sample Input 1:
-1
2

Sample Output 1:
числа -1 и 2 имеют разные знаки

Sample Input 2:
1
3

Sample Output 2:
числа 1 и 3 имеют одинаковые знаки

Напишите программу. Тестируется через stdin → stdout
Верно решили 169 учащихся
Из всех попыток 62% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {       
    let mut a: i8 = input(); 
    let mut b: i8 = input();  
    //a = -50;
    //b = -60;
    if (a>>7) ^ (b >>7) == 0
    {
        println!("числа {a} и {b} имеют одинаковые знаки");
    }
    else
    {
        println!("числа {a} и {b} имеют разные знаки");    
    }      
} 

