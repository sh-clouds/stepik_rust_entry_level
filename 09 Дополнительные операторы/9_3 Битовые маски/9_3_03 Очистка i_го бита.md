Очистка i-го бита 🪤🪤

Напишите программу, которая считывает два целых числа x (i8) и n (u8), сбрасывает n-й бит переменной x и выводит получившееся число в двоичной и десятичной записи в виде сообщений:

x до очистки {n}-го бита
в двоичной записи: {:b}
в десятичной записи: {}

x после очистки {n}-го бита
в двоичной записи: {:b}
в десятичной записи: {}
Тестовые данные ✅

Sample Input:
-1
7

Sample Output:
x до очистки 7-го бита
в двоичной записи: 11111111
в десятичной записи: -1

x после очистки 7-го бита
в двоичной записи: 01111111
в десятичной записи: 127

Напишите программу. Тестируется через stdin → stdout
Верно решили 74 учащихся
Из всех попыток 75% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {       
    let mut x: i8 = input();
    let mut n: u8 = input();
    println!("x до очистки {}-го бита", n); 
    println!("в двоичной записи: {:08b}", x);     
    println!("в десятичной записи: {}\n", x); 

    x &= !(1 << n);
    println!("x после очистки {}-го бита", n); 
    println!("в двоичной записи: {:08b}", x);     
    println!("в десятичной записи: {}\n", x);     
}