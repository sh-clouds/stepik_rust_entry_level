Проверка установки i-го бита 🪤🪤

Напишите программу, которая считывает два целых числа x (u8) и n (u8), проверяет на установку n-й бит переменной x и выводит получившееся число в двоичной записи в виде сообщения:

{n}-й бит числа {:b} равен {}
Тестовые данные ✅

Sample Input:
170
0

Sample Output:
0-й бит числа 10101010 равен 0

Напишите программу. Тестируется через stdin → stdout
Верно решили 72 учащихся
Из всех попыток 66% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {       
    let mut x: u8 = input();
    let mut n: u8 = input();
    println!("{n}-й бит числа {x:b} равен {}", (x & (1 << n))>>n);    
}