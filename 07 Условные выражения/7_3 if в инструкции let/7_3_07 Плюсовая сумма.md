Плюсовая сумма

В редакторе кода представлен кортеж значений. Дополните код и выведите (до 2 знаков) сумму всех положительных значений.

Напишите программу. Тестируется через stdin → stdout
Верно решили 111 учащихся
Из всех попыток 36% верных

use std::io;

fn main() {
    let tup = (1, 3.14, -12.3, -50, 100, 250, -4, 7.6);
    let mut sum = 0.0;
    let mut p: usize;

    let arr = [
        tup.0 as f64,
        tup.1 as f64,
        tup.2 as f64,
        tup.3 as f64,
        tup.4 as f64,
        tup.5 as f64,
        tup.6 as f64,
        tup.7 as f64,
    ];
    for i in arr {
        if i > 0.0 {
            sum += i;
        }
    }
    println!("{sum}", )     
}
}