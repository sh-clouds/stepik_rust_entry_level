Невисокосные дни 📆

Напишите программу, которая считывает номер месяца (u8) в диапазоне от 1 до 12 (1 январь, 2 февраль и т. д.) и выводит количество дней в этом месяце для невисокосного года.

Тестовые данные ✅

Sample Input:
1

Sample Output:
31

Напишите программу. Тестируется через stdin → stdout
Верно решили 123 учащихся
Из всех попыток 74% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {    
    let i1: usize = input();
    let m = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
    println!("{}", m[i1-1])   
}

