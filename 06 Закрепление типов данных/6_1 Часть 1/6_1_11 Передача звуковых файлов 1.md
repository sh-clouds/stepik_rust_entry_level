Передача звуковых файлов 1

Напишите программу, которая считывает четыре целых (u32) числа:

скорость передачи голосового сообщения (бит/с) записанное в стерео (2 канала) формате;
глубину кодирования бит) на отсчет;
частоту дискретизации (отсчеты в секунду);
время записи (в секундах).
Выведите время (в секундах) передачи голосового сообщения, если голосовое сообщение записано в стерео формате (формат звука), как показано в примере.

Подсказка 💡

Время передачи голосовой записи можно найти, разделив объем передаваемого файла на скорость передачи:

время передачи = формат звука * глубина кодирования * частота дискретизации * время записи / скорость передачи


Тестовые данные ✅

Sample Input:
64000
24
16000
90

Sample Output:
Время передачи голосового сообщения: 1080 секунд


use std::io;
use std::str::FromStr;
use std::fmt::Debug;
fn read_number<T: FromStr>() -> T where <T as FromStr>::Err: Debug {
    let mut user_input = String::new();
    io::stdin().read_line(&mut user_input).expect("Не удалось прочитать ввод");
    user_input.trim().parse().expect("Не удалось преобразовать в число")
}

fn main() {
    let u1: u32 = read_number(); // скорость передачи
    let u2: u32 = read_number(); // глубину кодирования (бит) на отсчет 
    let u3: u32 = read_number(); // частоту дискретизации  (отсчеты в секунду) 
    let u4: u32 = read_number(); // время записи  (в секундах)    
    println!("Время передачи голосового сообщения: {} секунд", 
        u2 * u3 * u4 * 2 / u1);
}