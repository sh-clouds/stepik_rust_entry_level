Правильный домофон 2.0 

Напишите программу, которая считывает три (u16) числа: количество этажей в доме, количество квартир на каждом этаже и номер квартиры. Затем программа определяет, в каком подъезде и на каком этаже этого дома находится квартира с заданным номером. Предполагается, что в доме нет строительных особенностей, таких как пропуски этажей или квартир, а также что нумерация квартир начинается с 1.

Гарантируется, что номер квартиры не превышает общее количество квартир в доме.

Подсказка 💡

Чтобы определить номер подъезда и этажа квартиры, можно воспользоваться формулами:

Номер подъезда:
подъезд = (номер_квартиры - 1) / (количество_этажей * количество_квартир_на_этаже) + 1

Номер этажа в подъезде:
этаж_в_подъезде = ((номер_квартиры - 1) / количество_квартир_на_этаже) % количество_этажей + 1

Тестовые данные ✅

Sample Input:
5
4
21

Sample Output:
Квартира с номером 21 находится в подъезде 2 на 1 этаже

Напишите программу. Тестируется через stdin → stdout
Верно решил 151 учащийся
Из всех попыток 50% верных


use std::io;
use std::str::FromStr;
use std::fmt::Debug;
fn read_number<T: FromStr>() -> T where <T as FromStr>::Err: Debug {
    let mut user_input = String::new();
    io::stdin().read_line(&mut user_input).expect("Не удалось прочитать ввод");
    user_input.trim().parse().expect("Не удалось преобразовать в число")
}

fn main() {
    let u1: u16 = read_number(); 
    let u2: u16 = read_number();   
    let u3: u16 = read_number();      
    println!("Квартира с номером {} находится в подъезде {} на {} этаже", 
        u3, 
        (u3 - 1) / (u1 * u2) + 1, 
        ((u3 - 1) / u2) % u1 + 1);
}

