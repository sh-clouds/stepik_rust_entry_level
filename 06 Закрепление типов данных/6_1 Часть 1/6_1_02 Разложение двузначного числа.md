Разложение двузначного числа

Напишите программу, которая считывает двузначное число (u8) и выводит его цифры, а именно единицы и десятки.

Чтобы найти десятки и единицы в двузначном числе, можно воспользоваться двумя математическими операциями: делением нацело и взятием остатка от деления. Подумайте, как можно применить эти операции в вашей программе для вычисления десятков и единиц введенного двузначного числа.

Тестовые данные ✅

Sample Input:
10

Sample Output:
1
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 217 учащихся
Из всех попыток 90% верных


use std::io;
use std::str::FromStr;
use std::fmt::Debug;
fn read_number<T: FromStr>() -> T where <T as FromStr>::Err: Debug {
    let mut user_input = String::new();
    io::stdin().read_line(&mut user_input).expect("Не удалось прочитать ввод");
    user_input.trim().parse().expect("Не удалось преобразовать в число")
}

fn main() {
    let i1: u8 = read_number(); 
    println!("{}\n{}", i1 / 10, i1 % 10);   
}