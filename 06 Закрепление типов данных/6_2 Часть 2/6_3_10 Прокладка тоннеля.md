Прокладка тоннеля

Рабочие прокладывают тоннель некоторой длины в метрах, ежедневно увеличивая норму прокладки на одно и то же число метров. Известно, что за первый день рабочие проложили определенную длину тоннеля в метрах.

Напишите программу, которая считывает три числа:

Sn - длину тоннеля в метрах (f64);
a1 - длину тоннеля, проложенную в первый день в метрах (f64);
n - количество потраченных дней на работу (u16).
Затем программа выводит сколько метров (до 3 знаков) тоннеля проложили рабочие в последний день, как показано в примере.

Подсказка 💡
Тестовые данные ✅

Sample Input:
450
2.5
20

Sample Output:
В последний день рабочие проложили 42.500 метр(ов)
Напишите программу. Тестируется через stdin → stdout
Верно решили 97 учащихся
Из всех попыток 93% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let f1: f64 = input(); // Sn - длину тоннеля в метрах (f64)    
    let f2: f64 = input(); // a1 - длину тоннеля, проложенную в первый день в метрах (f64) 
    let u: u16 = input();  // n - количество потраченных дней на работу (u16)        
    println!("В последний день рабочие проложили {:.3} метр(ов)", f1 * 2.0 / u as f64 - f2);
}


