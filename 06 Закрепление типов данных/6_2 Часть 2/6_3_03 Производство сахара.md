Производство сахара

Из сахарной свеклы получают сахар, масса которого составляет 18% массы свеклы. Напишите программу, которая считывает одно вещественное число: массу исходного сырья в тоннах и выводит в килограммах (до 3 знаков) сколько сахара получится при переработке, как показано в примере.

Тестовые данные ✅

Ps. темы 6 класса 🤭 

Sample Input:
1

Sample Output:
Из 1.000 тонн(ы) получится 180.000 кг сахара

Напишите программу. Тестируется через stdin → stdout
Верно решили 128 учащихся
Из всех попыток 78% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let f1: f64 = input();  
    println!("Из {:.3} тонн(ы) получится {:.3} кг сахара", f1, f1 * 180.0);
}
