Прогулка 🪁

Турист идет из одного города в другой каждый день, проходя больше, чем в предыдущий день, на одно и то же расстояние. Известно, что за первый день турист прошел некоторое расстояние в километрах.

Напишите программу, которая считывает четыре числа:

a1 - пройденное расстояние в км. в первый день (f64);
am - день, за которое необходимо рассчитать пройденное расстояние (u16);
n - количество потраченных дней на путь (u16);
Sn - расстояние между городами в км. (f64).

Затем программа выводит, сколько километров (до 3 знаков) прошел турист за требуемый день, как показано в примере.

Подсказка 💡
Тестовые данные ✅

Sample Input:
10
4
6
100

Sample Output:
За 4 день турист прошел 18.000 км

Напишите программу. Тестируется через stdin → stdout
Верно решили 97 учащихся
Из всех попыток 61% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let f1: f64 = input(); // a1 - пройденное расстояние в км. в первый день (f64)    
    let u1: u16 = input(); // m - день, за которое необходимо рассчитать пройденное расстояние (u16); 
    let u2: u16 = input(); // n - количество потраченных дней на путь (u16);     
    let f2: f64 = input(); // Sn - расстояние между городами в км. (f64)        
    println!("За {u1} день турист прошел {:.3} км",  
        f1 + (u1 - 1) as f64 * ( f2 * 2.0 - f1 * 2.0 * u2 as f64) / ((u2 - 1) * u2) as f64);
}
