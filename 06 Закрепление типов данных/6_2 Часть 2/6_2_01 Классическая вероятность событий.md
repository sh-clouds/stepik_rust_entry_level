Классическая вероятность событий

На экзамен вынесено какое-то количество вопросов. Руслан не выучил некоторые из них. Напишите программу, которая считывает два целых числа: количество вопросов и количество невыученных вопросов, а затем выводит вероятность (до 3 знаков) того, что Руслану попадется выученный вопрос, как показано в примере. 

Гарантируется, что второе число не превышает первое.

Подсказка 💡
Для вычисления вероятности того, что Руслану попадется выученный вопрос, мы можем использовать простое математическое выражение:

вероятность = количество выученных вопросов / общее количество вопросов

Тестовые данные ✅
Sample Input:
60
3

Sample Output:
Вероятность попадания выученного вопроса: 0.950

Напишите программу. Тестируется через stdin → stdout
Верно решили 135 учащихся
Из всех попыток 75% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let i1: i32 = input(); // количество вопросов
    let i2: i32 = input(); // количество невыученных вопросов    
    println!("Вероятность попадания выученного вопроса: {:.3}", (i1 - i2) as f64 / (i1 as f64) );
}