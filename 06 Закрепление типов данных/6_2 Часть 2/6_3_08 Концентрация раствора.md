Концентрация раствора ⚗️

В сосуд, содержащий определенный объем литров некоторого процентного водного раствора какого-то вещества, добавили еще дополнительные литры воды.

Напишите программу, которая считывает три вещественных числа: исходный объем в литрах, процент водного раствора и добавленные литры воды. Программа должна вывести концентрацию получившегося раствора (в % до 3 знаков) как показано в примере.

Подсказка 💡
Концентрация раствора равна: C = Vв−ва / Vр−ра ∗ 100%

Тестовые данные ✅

Sample Input:
7
14
9

Sample Output:
Концентрация получившегося раствора: 6.125%

Напишите программу. Тестируется через stdin → stdout
Верно решили 105 учащихся
Из всех попыток 78% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let f1: f64 = input(); // исходный объем в литрах   
    let f2: f64 = input(); // процент водного раствора 
    let f3: f64 = input(); // добавленные литры воды         
    println!("Концентрация получившегося раствора: {:.3}%", f2 / ((f1 + f3) / f1) );
}