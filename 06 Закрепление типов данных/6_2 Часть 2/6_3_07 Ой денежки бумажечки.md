Ой денежки бумажечки 💰

Руслан, Рустам, Солтан и Тимур учредили компанию с некоторым уставным капиталом в рублях. Руслан внес определенный процент уставного капитала, Рустам - какое-то количество рублей, Солтан  - некоторую часть уставного капитала, а оставшуюся часть капитала внес Тимур. Учредители договорились делить ежегодную прибыль пропорционально внесенному в уставной капитал вкладу.

Напишите программу, которая считывает пять вещественных чисел: уставной капитал, внесенный процент Руслана, количество рублей Рустама, внесенная часть Солтана (от 0 до 1) и прибыль. Программа должна вывести сумму от прибыли (до 3 знаков) которая причитается Тимуру. Ответ дайте в рублях, как показано в примере.

Тестовые данные ✅
Sample Input:
300000
16
50000
0.15
1000000

Sample Output:
От прибыли 1000000 рублей Тимуру причитается 523333.333 рублей

Напишите программу. Тестируется через stdin → stdout
Верно решили 109 учащихся
Из всех попыток 68% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let f1: f64 = input(); // уставной капитал    
    let f2: f64 = input(); // внесенный процент Руслана 
    let f3: f64 = input(); // количество рублей Рустама   
    let f4: f64 = input(); // внесенная часть Солтана (от 0 до 1)     
    let f5: f64 = input(); // прибыль    
    
    println!("От прибыли {} рублей Тимуру причитается {:.3} рублей", 
        f5 as i32,
        (f1 - f1 * f2 / 100.0 - f3 - f1 * f4) * f5 / f1 );
}

