Замок Знаний

В Древнем Замке Знаний 🏰 хранится удивительная книга, содержащая тайны арифметики. Однажды плохо учившийся ученик похитил эту книгу и разорвал её на две части. Теперь книга разделена на "Строку Первых Чисел" и "Строку Вторых Чисел". Лишь тот, кто сможет правильно соединить эти строки, сможет постичь тайны чисел и обрести большие знания. Ваша программа станет ключом к объединению этих частей и восстановлению книги.

Напишите программу, которая считывает два целых числа и выводит основные арифметические операции для них: сложение, вычитание, умножение, деление и нахождение остатка от деления. Гарантируется, что второе число не равно 0!

Тестовые данные ✅

Sample Input:
2
4

Sample Output:
2 + (4) = 6
2 - (4) = -2
2 * (4) = 8
2 / (4) = 0
2 % (4) = 2

Напишите программу. Тестируется через stdin → stdout
Верно решили 417 учащихся
Из всех попыток 73% верных


use std::io;

fn main() {
    // put your Rust code here
    let mut s1 = String::new();
    let mut s2 = String::new();    
    
    io::stdin()
        .read_line(&mut s1) 
        .expect("Не удалось прочитать ввод"); 

    io::stdin()
        .read_line(&mut s2) 
        .expect("Не удалось прочитать ввод");   
    
    let num1: i32;
    let num2: i32;    
    num1 = s1
        .trim()
        .parse() 
        .expect("Пожалуйста, введите число!");
    
    num2 = s2
        .trim()
        .parse() 
        .expect("Пожалуйста, введите число!");    
  
    println!("{} + ({}) = {}", num1, num2, num1 + num2);
    println!("{} - ({}) = {}", num1, num2, num1 - num2);
    println!("{} * ({}) = {}", num1, num2, num1 * num2);
    println!("{} / ({}) = {}", num1, num2, num1 / num2);
    println!("{} % ({}) = {}", num1, num2, num1 % num2);
}
