Символьный тип данных

Самым примитивным алфавитным типом Rust является символьный тип (character type) char, предназначенный для отдельных символов:

fn main() {
    let ch = 'c';
    let ann_ch: char = 'c';
}

В отличие от строковых литералов, которые используют двойные кавычки "" символьные литералы заключаются в одинарные ''. Тип char имеет размер 4 байта и представляет собой скалярное значение Unicode, что значит мы можем хранить не только ASCII символы, но и множество других:

диакритические знаки:

fn main() {
    let ch = 'ặ';
}

символы множества языков мира, закодированные в юникоде:

fn main() {
    let ch = '大';
    let kor = '조';
    let jap = '字';
}

эмодзи:

fn main() {
    let em = '🥲';
}

управляющие символы:

fn main() {
    let em = '\n';
}

И т.д.

Подробнее рассматривается в следующих частях 😿. 