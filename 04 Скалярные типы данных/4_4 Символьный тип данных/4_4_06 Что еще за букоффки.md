Что еще за букоффки ?

Напишите программу, которая считывает пять строк, представленные символами и выводит их в одну строку.

Тестовые данные ✅

Sample Input:
こ
ん
に
ち
は

Sample Output:
こんにちは

Напишите программу. Тестируется через stdin → stdout
Верно решил 291 учащийся
Из всех попыток 67% верных


use std::io;

fn main() {
    let mut smile1 = String::new();
    let mut smile2 = String::new();
    let mut smile3 = String::new();
    let mut smile4 = String::new();
    let mut smile5 = String::new();
    io::stdin().read_line(&mut smile1).expect("biba");
    io::stdin().read_line(&mut smile2).expect("biba");
    io::stdin().read_line(&mut smile3).expect("biba");
    io::stdin().read_line(&mut smile4).expect("biba");
    io::stdin().read_line(&mut smile5).expect("biba");
    let smile1: char = smile1.trim()
                            .parse()
                            .expect("boba");
    let smile2: char = smile2.trim()
                            .parse()
                            .expect("boba");
    let smile3: char = smile3.trim()
                            .parse()
                            .expect("boba");
    let smile4: char = smile4.trim()
                            .parse()
                            .expect("boba");
    let smile5: char = smile5.trim()
                            .parse()
                            .expect("boba");
    println!("{}{}{}{}{}", smile1, smile2, smile3, smile4, smile5) 
}

