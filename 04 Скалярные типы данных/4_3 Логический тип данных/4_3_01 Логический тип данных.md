Логический тип данных

Данная тема будет самой короткой из всех 🙃. Когда перед программистом возникает какой-то вопрос, требующий ответа да или нет, то на помощь в хранении подобных ответов приходит логический тип (boolean type) bool, имеющий только два значения true и false:

fn main() {
    let t_a: bool = true;
    let f_a: bool = false;

    let t = true;
    let f = false;
}


Название логический тип bool получил в честь английского математика и логика Джорджа Буля. Поэтому данные типы ещё называют булев или булевый тип.

Самостоятельно они редко используются, в основном применяются вместе с условиями, которые рассмотрим в модуле 7.