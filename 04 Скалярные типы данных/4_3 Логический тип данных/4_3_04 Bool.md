Bool

Напишите программу, которая создает две переменные, инициализирует их значениями true и false и выводит их значения следующим образом:

Вам нравится программировать ? true
Вы ленитесь делать задания ? false

Sample Input:

Sample Output:
Вам нравится программировать ? true
Вы ленитесь делать задания ? false

Напишите программу. Тестируется через stdin → stdout
Верно решили 315 учащихся
Из всех попыток 90% верных


fn main() {
    let t_a: bool = true;
    let f_a: bool = false;
    println!("Вам нравится программировать ? {}", t_a);
    println!("Вы ленитесь делать задания ? {}", f_a);    
}