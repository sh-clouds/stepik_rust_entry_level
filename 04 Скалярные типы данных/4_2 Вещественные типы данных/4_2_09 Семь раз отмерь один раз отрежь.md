Семь раз отмерь один раз отрежь

В программировании точность играет важную роль. Действительно, следить за точностью критически важно во многих областях. Рассмотрим, например, процесс изготовления инженерных деталей.

Предположим, что производится изготовление важной детали для двигателя. Допустим, данная деталь должна иметь диаметр 10.000 миллиметров с допустимым отклонением ±0.002 миллиметра. Это означает, что диаметр детали должен находиться в диапазоне от 9.998 до 10.002 миллиметров.

Если диаметр детали будет меньше 9.998 миллиметров, то это может привести к несовместимости с другими компонентами двигателя или даже к потенциальному отказу в работе. Если диаметр детали превысит 10.002 миллиметра, это может вызвать трение или неполадки в работе двигателя, что также может представлять угрозу для безопасности.

Таким образом, следить за точностью в процессе изготовления этой детали критически важно для обеспечения безопасности и надежности эксплуатируемых систем. Это касается всех областей: медицинского оборудования, производства лекарств, продуктов, удобрений и т.д.

Теперь взглянем на задание. Напишите программу, которая считывает вещественное и целое (usize) число , а затем выводит дробное число с точностью до числа знаков после запятой, указанного введённым целым числом. Давайте проверим, насколько хорошо ваша программа справится с этой ответственной задачей!

Тестовые данные ✅

Sample Input:
5.123456
5

Sample Output:
5.12346

Напишите программу. Тестируется через stdin → stdout
Верно решили 319 учащихся
Из всех попыток 68% верных


fn read_int() -> i32 {
    let mut strnum = String::new();
    std::io::stdin().read_line(&mut strnum).expect("Could not read the input.");
    let num = strnum.trim().parse().expect("Incorrect input. Please enter the number");
    return num;
}

fn read_float() -> f32 {
    let mut strnum = String::new();
    std::io::stdin().read_line(&mut strnum).expect("Could not read the input.");
    let num = strnum.trim().parse().expect("Incorrect input. Please enter the number");
    return num;
}

fn main() {
    let num = read_float();
    let precision = read_float() as usize;
    
    println!("{:.precision$}", num);
}
