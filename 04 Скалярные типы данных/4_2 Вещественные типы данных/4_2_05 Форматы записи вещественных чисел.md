Форматы записи вещественных чисел

Rust поддерживает несколько способов записи вещественных чисел:

Стандартная:
let d_pi = 3.141592; // f64

Суффиксная:
let s_pi = 3.1415_f32; // f32
s_pi = 3.1415f32; // аналогично

let g = 9_f32 // 9.0
g = 9f32 // аналогично

Экспоненциальная (научная):
let d_pi = 3.14159E+6; // d_pi = 3141590
d_pi = 3.14159e+6; // аналогично

let s_pi = 3.14E-4_f32; // s_pi = 0.000314, f32
s_pi = 3.14e-4f32; // аналогично

Для вывода числа в научной записи используйте спецификатор формата :e или :E:

fn main() {
    let mut d_pi = 3.14159E+6;
    println!("{:E}, {:e}", d_pi, d_pi); // "3.14159E6, 3.14159e6"

    d_pi = 3141590.0;
    println!("{:E}, {:e}", d_pi, d_pi); // "3.14159E6, 3.14159e6"

    let mut s_pi = 3.14E-4_f32;
    println!("{:E}, {:e}", s_pi, s_pi); // "3.14E-4, 3.14e-4"

    s_pi = 0.000314;
    println!("{:E}, {:e}", s_pi, s_pi); // "3.14E-4, 3.14e-4"
}

Также можно настроить количество знаков после запятой, регулируя ширину строки. Как литерально:

fn main() {
    let mut float = 513.0;
    println!("{:.2}, {:.2}", float, float); // "513.00, 513.00"

    float = 0.513;
    println!("{:.2}, {:.2}", float, float); // "0.51, 0.51"
}

Так и динамически, указав ширину в списке параметров и добавив постфикс $, указывающий на то, что параметр prec является значением типа usize, задающий ширину:

fn main() {
    let mut float = 513.0;
    let mut prec = 3;

    println!("{:.prec$}, {:.prec$}", float, float); // "513.000, 513.000"
    println!("{:.2$}, {:.2$}", float, float, prec); // "513.000, 513.000"

    float = 0.513;
    println!("{:.prec$}, {:.prec$}", float, float); // "0.513, 0.513"
}

Подход с использованием синтаксиса доллара не влияет на счетчик "следующего аргумента". Поэтому обычно рекомендуется ссылаться на аргументы по позиции или использовать именованные аргументы.
Можно сразу соединить кол-во знаков и спецификатор типа. В этом случае просто будут добавляться незначащие нули:

fn main() {
    let mut float = 513.0;
    let mut prec = 3;

    println!("{:.prec$E}, {:.prec$e}", float, float); // "5.130E2, 5.130e2"

    float = 0.513;
    prec = 10;

    println!("{:.prec$E}, {:.prec$e}", float, float); // "5.1300000000E-1, 5.1300000000e-1"
}

Rust использует округление до ближайшего чётного, что является режимом округления по умолчанию в IEEE 754:

fn main() {
    println!("{:.2}", 12.126); // "12.13"

    println!("{:.2}", 12.125); // "12.12"

    println!("{:.2}", -12.125); // "-12.12"

    println!("{:.2}", -12.126); // "-12.13"

    println!("{0:.1$e}", 12345, 3); // "1.234e4"

    println!("{0:.1$e}", 12355, 3); // "1.236e4"
}

Точечная:
let g = 9.; // g = 9.0, f64

Но не:
let g = 9.f32; // ошибка
let g: f32 = 9.;


 Особенности вещественной арифметики

Вещественные числа представлены в компьютерах по основанию 2 и не хранят в себе целое число десятичных цифр. Так, дробь 0.625 в десятичном виде представлена как 6/10 + 2/100 +5/1000, и та же дробь в двоичной 0.101 выглядит как 1/2 + 0/4 + 1/8.

Почему 0.625 равно 0.101?

Большинство десятичных дробей невозможно в точности представить в двоичном виде. В следствии этого мы получаем их приближенные значения. Поэтому при попытке посчитать выражение 10.5−7.4, мы можем столкнуться с такой ситуацией:

fn main() {
    let difference = 10.5 - 7.4; // 3.0999999999999996
}

Однако не всегда такое происходит. Например, выражение 
0.1 + 0.4 посчитается правильно:

fn main() {
    let difference = 0.1 + 0.4; // 0.5
}

В этом примере результат 0.5 может быть представлен точно и ошибки округления во входных числах могут компенсировать друг друга. 

В других случаях, как для 0.1 + 0.3 значение в компьютере на самом деле не равно 0.4, но достаточно близко к нему чем к любому другому вещественному числу. Поэтому многие языки отображают наиболее близкое число вместо преобразования фактического результата обратно в ближайшую десятичную дробь.

Обратите внимание, что это заложено в самих вещественных числах в двоичном представлении: это не ошибка Rust и не ошибка в коде. Такое поведение присутствует и в других языка программирования, которые поддерживают вещественную арифметику.

Для разрешения таких ситуаций используйте:

Для научных вычислений библиотеки точной арифметики, реализующие десятичную арифметику или арифметику на основе рациональных чисел;

Для обычных вычислений можете игнорировать или округлять до ожидаемого количества десятичных цифр. В таком случае при выводе выставите нужное количество знаков:

fn main() {
    let difference = 10.5 - 7.4; // 3.0999999999999996
    println!("{:.2}", difference); // "3.10"
}


Переполнение вещественных типов

Согласно стандарту IEEE 754 при превышении предельного значения будет возвращено значение inf от infinity:

fn main() {
    let p_inf = 1.79769e+308 * 1.79769e+308; // inf
    let n_inf = -1.79769e+308 * 1.79769e+308; // -inf
}

Некоторые специальные значения вещественных типов:

Деление на ноль:
fn main() {
    let p_inf = 1.0 / 0.0; // inf
    let n_inf = -1.0 / 0.0; // -inf
}

Вычисление неопределенных значений таких как квадратный корень отрицательного числа:

fn main() {
    let p_inf = (-1.0).sqrt(); // NaN
}

Отрицательный ноль 🤭:

fn main() {
    let mut n_zero = -0.0; // -0

    n_zero = -0.0 + 1.0; //  1
    n_zero = -0.0 - 1.0; // -1
    n_zero = -0.0 + 0.0; //  0
    n_zero = -0.0 - 0.0; // -0

    n_zero = -0.0 * 0.0; // -0
    n_zero = -0.0 * -0.0; //  0

    n_zero = -0.0 / 1.0; // -0
    n_zero = -0.0 / -1.0; //  0
}

По умолчанию всегда +0.0, т.е. обычный ноль 0:

fn main() {
    let mut n_zero = 1.0 - 1.0; // 0
    n_zero = -1.0 + 1.0; // 0