Руководство по стилю Rust 💈

Командой Rust было создано замечательное руководство по стилю форматирования исходного кода. Давайте же прочитаем его.

Форматирование кода - это по большей части механическая задача, требующая как времени, так и умственных усилий. Используя инструмент автоматического форматирования (форматтер) rustfmt, программист освобождается от этой задачи и может сосредоточиться на более важных вещах.

Более того, придерживаясь данного руководства по стилю, программистам не нужно создавать специальные правила стиля, а также обсуждать это с другими программистами, что экономит время, ресурсы на общение и умственную энергию.

Мы воспринимаем информацию посредством сопоставления с образцом. Благодаря тому, что весь код Rust имеет одинаковый стиль, то для понимания стороннего кода требуется меньше усилий, что снижает входной барьер для новых разработчиков.

Таким образом, использование инструмента форматирования rustfmt экономит ваше время, а настройки по умолчанию помогают сохранить это на уровне всего сообщества.

Чтобы воспользоваться rustfmt в игровой площадке, достаточно зайти в TOOLS -> Rustfmt:



Для VSCode, находясь в одной директории с исходным файлом, вызовите в терминале: rustfmt --check исходный_файл.rs



Если вы хотите, чтобы в VSCode после каждого сохранения кода автоматически вызывался rustfmt и применялись редактирования по стилю, добавьте в файл настроек settings.json расширения code runner следующие строки:

"[rust]": {
    "editor.defaultFormatter": "rust-lang.rust-analyzer",
    "editor.formatOnSave": true,
    "editor.formatOnSaveMode": "file"
    },

                  
Для этого зайдите в Settings VSCode и наберите code-runner.executorMap:



После нажмите Edit in settings.json и вставьте приведённый выше текст:



Стиль по умолчанию

Руководство Rust определяет стиль по умолчанию и рекомендует следовать ему. Используйте в качестве справочника руководство по стилю и инструмент форматирования (форматтер) rustfmt. Важно отметить, что это не означает запрета на использование вами собственного стиля.

Большая часть руководства представлена здесь, оставшаяся будет включаться в следующие части по мере необходимости. Незнакомые темы оставляйте и возвращайтесь к ним после изучения. 



Соглашения о форматировании

Комментарии
Следующие рекомендации по комментариям носят исключительно рекомендательный характер. Форматтер стиля может пропускать форматирование комментариев.

Рекомендуется использовать однострочные комментарии // вместо блочных /* ... */:
fn main () {} // ok

fn main () {} /* nope */

                  
При написании комментариев к строке рекомендуется поставить один пробел после открывающего знака комментария:
fn main () {} // ok

fn main () {} //nope

                  
При использовании однострочных блочных комментариев рекомендуется ставить один пробел после открывающего знака и перед закрывающим знаком комментария:
fn main () {} /* ok */

fn main () {} /*nope*/

                  
Для многострочных блочных комментариев рекомендуется размещать новую строку после открывающего знака комментария и перед закрывающим знаком:
/*
   Это многострочный комментарий блока,
   который размещается на отдельных строках
   как перед закрывающим, так и после открывающего знака.
*/

fn main () {}

                  
Предпочтительно размещать комментарий в отдельной строке:
// ok
fn main () {}

                  
Если комментарий следует за кодом, перед ним следует добавить один пробел:
fn main () {} // ok

fn main () {}// nope

                  
Если блочный комментарий отображается внутри строки, используйте окружающие его пробелы, как если бы это был идентификатор или ключевое слово. Ниже приведён пример исключительно для демонстрации. Не применять!:
fn /* ok */ main() {}

fn/* nope */main() {}

                  
Не добавляйте пробелы в конце однострочного комментария или в конце строки многострочного комментария. Обозначим пробел символом _:

/*
    nope_
*/

fn main () {} // ok

fn main () {} // nope_

                  
Комментарии обычно должны представлять собой полные предложения, начинающиеся с заглавной буквы и заканчивающиеся точкой. Однако блочный комментарий можно рассматривать как примечание без знаков препинания:
/*
    Этот блок комментария объясняет
    как работает определенная часть кода
    и не содержит знаков препинания
*/

fn main () {}

                  
Строка, полностью представляющая собой комментарий, должна быть ограничена:
Либо 80 символами, включая символы комментариев, но исключая отступы
// Размер этого комментария не превышает ...................... 80 символов.

{
    // Ширина этого комментария составляет ............................ 80 символов.
}

                  
Либо максимальной шириной строки, включая символы комментариев и отступы:
{
    {
        {
            {
                {
                    {
                        // Этот комментарий ограничен размером в ..................... 100 символов.
                    }
                }
            }
        }
    }
}

                  
В зависимости от того, что меньше !



Отступ и ширина строки

Используйте пробелы, а не табуляции. Для наглядности выделите отступы перед каждым комментарием:

fn main() {
    // ok

	// nope
}

                  
Каждый уровень отступа должен составлять 4 пробела, т.е. все отступы за пределами строковых литералов и комментариев должны быть кратны 4:


Максимальная ширина строки - 100 символов.



Блочный отступ

Предпочитайте блочный отступ визуальному отступу:

// Блочный отступ, ok
a_function_call(
    foo,
    bar,
);

// Визуальный отступ, nope
a_function_call(foo,
                bar);

                  
Это приводит к уменьшению различий (например при переименовании a_function_call в приведённом выше примере) и меньшему смещению вправо:

// ok
function_call(
    foo,
    bar,
);

// nope
function_call(foo,
                bar);

                  

Завершающие запятые

В списках любого типа, разделённых запятыми, используйте конечную запятую, после которой следует новая строка:

// ok
function_call(
    argument,
    another_argument,
);

// ok
let array = [
    element,
    another_element,
    yet_another_element,
];

                  
Это упрощает перемещение кода (например путём копирования и вставки) и уменьшает размеры различий, поскольку добавление или удаление элементов не требует изменения другой строки для добавления или удаления запятой:

// nope
function_call(
    argument, another_argument,
);

// nope
let array = [
    element, another_element,
    yet_another_element,
];

           

Пустые строки

Разделяйте элементы и инструкции одной или двумя символами новой строки:

fn foo() {
    let x = ...;

    let y = ...;
}

                  
Либо без разделения:

fn foo() {
    let y = ...;
    let z = ...;
}

fn bar() {}
fn baz() {}