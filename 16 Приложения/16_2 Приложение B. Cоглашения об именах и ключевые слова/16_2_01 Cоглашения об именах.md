Cоглашения об именах

В Rust необходимо придерживаться следующих простых правил именования:

В именах разрешено использовать буквы любого языка 🫣, тем не менее используйте латинские a-z, A-Z, 0-9 и символ нижнего подчеркивания _;

Имена не должны начинаться с цифры или специальных символов, кроме подчеркивания; 

Символы в верхнем и нижнем регистре рассматриваются, как разные;

Rust использует змеиный регистр https://ru.wikipedia.org/wiki/Snake_case как основной стиль для имён, функций и переменных. Так, имя переменной пишется строчными буквами birth. Если же имя состоит из нескольких слов, то принято разделять слова символом нижнего подчеркивания _, birth_date;

В качестве имени нельзя использовать ключевые слова Rust;

Не рекомендуется начинать с символа нижнего подчеркивания _, т.к. добавление подчеркивания в начале имени переменной приводит к её игнорированию компилятором, если она не используется.

На длину имени не накладывается никаких ограничений и все символы в имени являются значащими.



Примеры допустимых и недопустимых имен в Rust.

  let cat;       // допустимое

  let Cat;       // допустимое, но не соответствует соглашению

  let CAT;       // допустимое, но не соответствует соглашению

  let my_cats1;   // допустимое

  let _my_cats1;  // допустимое, но имеет особый смысл

  let 3ree;      // ошибка, начинается с цифры
  
  let +tree;      // ошибка, содержит специальный символ +

  let win11;      // допустимое

  let struct;    // ошибка, struct является ключевым словом

  let begin;     // допустимое

  let __fools;   // допустимое, но имеет особый смысл

  let this_is_long_long_long_long_variable;    // допустимое

  let one-two;   // ошибка, дефисы не разрешены

   
                  
Ключевые слова в Rust.

as	else	in	ref	true	become	typeof
async	enum	let	return	type	box	unsized
await	extern	loop	Self	union	do	virtual
break	false	match	self	unsafe	final	yield
const	fn	mod	static	use	macro	 
continue	for	move	struct	where	override	 
crate	if	mut	super	while	priv	 
dyn	impl	pub	trait	abstract	try	 
