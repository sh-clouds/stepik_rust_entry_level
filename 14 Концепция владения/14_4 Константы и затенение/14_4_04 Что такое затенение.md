Что такое затенение ?

Выберите один вариант из списка
Верно решили 192 учащихся
Из всех попыток 94% верных
 Правильно.

Возможность создания новой переменной с тем же именем, что и уже существующая переменная, что приводит к "затенению" предыдущей переменной +

Способ предотвращения гонок данных при параллельном программировании

Процесс, при котором функция перекрывает глобальную переменную с тем же именем

Механизм обработки ошибок, позволяющий заменить значение переменной на значение по умолчанию в случае ошибки