Время работы аккумулятора 🔋

Напишите программу, которая считывает емкость аккумулятора (в Ампер-часах, Ah) f64, напряжение аккумулятора (в Вольтах, В) f64 и энергопотребление устройства (в Ваттах, Вт) f64. Программа должна и вывести оставшееся время работы аккумулятора.

Если остались минуты, вывести в виде: Аккумулятора хватит еще на {} мин;
Иначе, вывести в виде: Аккумулятора хватит еще на {} ч {} мин.
Формула для расчета времени работы аккумулятора (T) в часах:

T hours b = C * U / P,

где:

C - емкость аккумулятора в Ампер-часах (Ah);
U - напряжение аккумулятора в Вольтах (В);
P - энергопотребление устройства в Ваттах (Вт).

Тестовые данные ✅

Sample Input:
7
12
72

Sample Output:
Аккумулятора хватит еще на 1 ч 10 мин

Напишите программу. Тестируется через stdin → stdout
Верно решили 87 учащихся
Из всех попыток 52% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let c: f64 = input();
    let u: f64 = input();
    let p: f64 = input();
    
    let h: f64 = c * u / p;
    let m: f64 = h * 60.0;
    
    if h >= 1.0
    {
        println!("Аккумулятора хватит еще на {} ч {} мин", h as i64, (m % 60.0).round() as f64);        
    }
    else
    {
        println!("Аккумулятора хватит еще на {} мин", (m.round() as i64) % 60);
    }
}


