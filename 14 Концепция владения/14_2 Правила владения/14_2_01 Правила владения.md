Правила владения

Способы выделения памяти
Семантика перемещения
Клонирование
Семантика копирования
Владение и функции
Возвращение значений и область видимости



Способы выделения памяти

Все изученные нами типы данных имеют строго заданный размер, следовательно, они хранятся в стеке. Это позволяет легко размещать и извлекать их оттуда, а также быстро создавать их копии.

Перейдём теперь к рассмотрению динамически выделяемой памяти. Для дальнейшего изучения нам потребуется тип данных,  размещающийся на куче. С одним из таких мы вскользь познакомились в начале курса (ввод данных) строковый тип String. Мы сконцентрируемся на тех частях String, которые связаны с владением. Подробное изучение String оставим на следующую часть 🤓.

Как ранее было сказано, типы данных с изменяемым размером размещаются в куче. Это означает, что:

Выделение памяти происходит для всего содержимого с возможностью последующего расширения, а если не задано начальное значение, то будет выделен некоторый минимальный объём памяти;
Память, как и для стековых типов запрашивается у операционной системы и выделяется во время выполнения программы;
Возврат выделенной памяти операционной системе, при завершении работ.


Пункт первый

Ошибочно полагать, что если объём содержимого неизвестен, то и объём выделяемой памяти тоже. Нет!

При создании типов данных с изменяемым размером следует различать их длину (length) и ёмкость (capacity). Длина (length)  это непосредственное количество занимаемой памяти в байтах. Ёмкость (capacity) - это количество байтов, которые текущая выделенная память может содержать без необходимости выделения новой памяти. Если тип превышает свою емкость при добавлении данных, Rust автоматически увеличит ёмкость, выделив больше памяти и скопировав данные в новое место на куче, освобождая старую область памяти. Этот процесс называется перераспределением (reallocation).

Ёмкость обычно устанавливается на некоторое минимальное значение, которое может быть увеличено автоматически по мере добавления данных. Это позволяет избежать повторных выделений памяти при добавлении данных.

Точное начальное значение ёмкости зависит от способа выделения и реализации стандартной библиотеки Rust на конкретной платформе и архитектуре. На практике обычно выделяется небольшое количество памяти для хранения начальных данных, но точный объём может варьироваться.


Пункт второй

Запрос памяти может быть выполнен разными способами, от чего и будет зависеть выделяемый объём. Так, изменяемая строка String может быть создана как:

Пустая строка;
Строка из строчного литерала.
Многие уже догадались или помнят, что для создания пустой строки необходимо с помощью оператора области видимости :: вызвать ассоциированную функцию new() у типа String.

fn main() {
    let empt_str = String::new();

    // "capacity = 0, length = 0"
    println!(
        "capacity = {}, length = {}",
        empt_str.capacity(),
        empt_str.len()
    );
}

                  
Игровая площадка ⛱️

Ассоциированная функция new() просто создаёт новую пустую строку, в связи с чем её длина и емкость равны 0. Для демонстрации были вызваны соответствующие методы у empt_str.

Также строка может быть создана из строчного литерала. Для этого необходимо вызвать соответствующую ассоциированную функцию from() передав в качестве аргумента литерал:

fn main() {
    let empt_str = String::from("hello");

    // "capacity = 5, length = 5"
    println!(
        "capacity = {}, length = {}",
        empt_str.capacity(),
        empt_str.len()
    );
}

                  
Игровая площадка ⛱️

После запуска видно, что ёмкость и длина изменились и равны длине hello, что логично. Почему же тогда ёмкость не стала больше? Ответ прост, благодаря литералу hello компилятор на этапе компиляции заранее определил требуемый объём. В таком случае воспользуемся пользовательским вводом:

fn main() {
    let mut user_input = String::new();

    // hello
    std::io::stdin().read_line(&mut user_input);

    // "capacity = 8, length = hello + \n = 6"
    println!(
        "capacity = {}, length = {}",
        user_input.capacity(),
        user_input.len()
    );
}

                  
Игровая площадка ⛱️

Теперь, введя то же слово hello, ёмкость и длина уже различны. Надеемся теперь стало понятно.


Пункт третий

Если предыдущие два пункта довольно похожи в других языках программирования, то третий отличается.

Исторически, возврат выделенной памяти было сложной задачей. Традиционно существует два подхода:

Языки, использующие сборщик мусора (JavaScript, Python, Go и т.д);
Языки с ручным управлением памятью (C, C++ и т.д.).
В языках из первой группы за память, которая больше не используется, отвечает сборщик мусора, который это отслеживает и очищает автоматически. Вторые же перекладывают эту работу на программистов 🥲. Конечно есть умные указатели, но не будем пока об этом. Программисты сами обязаны определять момент освобождения и вызывать соответствующий для этого код, подобно тому, как мы выделяли память для empt_str. В случае неосвобождения памяти произойдёт её утечка, и она будет потеряна. Если освободить память раньше, чем завершиться её использование, то последующее обращение к ней может привести к ошибкам выполнения программы. Сделать это дважды тоже будет ошибкой. Должно быть одно выделение и освобождение памяти.

В Rust был применён иной подход. Память автоматически возвращается, как только владеющая памятью переменная выходит из области видимости, подобно тому, как происходит со стековой переменной:

fn main() {

    {
                                                 // переменной heap_str еще не существует
        let heap_str = String::from("hello");    // heap_str определена и может использоваться

        //heap_str все еще доступна

    }   // переменной heap_str больше не существует
}

                  
Когда переменная heap_str выходит за пределы области видимости, Rust автоматически вызывает метод drop (не может быть вызван явно) после закрывающей фигурной скобки } и память освобождается.

Важно понять данных подход, поскольку он оказывает глубокое влияние на способ написания кода в Rust. В более сложных ситуациях может возникнуть необходимость в наличии нескольких переменных, которые ссылаются на одни и те же данные в куче, и тогда поведение кода может быть неожиданным. Изучим несколько таких ситуаций.



Семантика перемещения

Рассмотрим, как несколько переменных по-разному взаимодействуют с одними и теми же данными в Rust. Начнём с типов,  размещаемых на стеке:

fn main() {
    let x = 5; // x = 5
    let y = x; // y = 5

    println!("x = {x}, y = {y}"); // "x = 5, y = 5"
}

                  
Логика программы проста. Сначала переменная x инициализируется значением 5, а затем в y заносится её копия. Теперь у нас есть две копии значения 5.

Теперь рассмотрим версию с типом на куче String:

fn main() {
    let s1 = String::from("hello"); // s1 = "hello"
    let s2 = s1; // s2 = ?
}

                  
На первый взгляд всё то же самое, s1 инициализируется строкой hello и копируется в s2, но нет. Давайте посмотрим, как s1 выглядит изнутри:



Наша строка s1 состоит из трёх частей:

ptr (pointer) - указатель на память, в которой хранится содержимое строки;
len - длина;
capacity - ёмкость.
Эта группа данных хранится в стеке. Справа - память в куче, в которой хранится содержимое строки. Когда мы присваиваем s1 переменной s2, то копируются не данные в куче, на которые указывает указатель, а данные стека, т.е.  указатель, длина и ёмкость. Представление данных в памяти выглядит так:

Почему же не копировать данные кучи подобно данным стека ? Тогда у каждой переменной была бы своя копия. Ответ на самом деле прост. Для маленьких данных на куче операция s2 = s1 не так трудозатратна, но для больших это очень дорого с точки зрения производительности во время выполнения.

Ранее мы сказали, что когда переменная выходит за пределы области видимости, Rust автоматически вызывает метод drop и освобождает память данной переменной. Но на предыдущем рисунке оба указателя ссылаются на одно и то же место. Что же произойдет при выходе из области видимости обеих переменных s1 и s2 ? Ошибка ? Да, произойдет ошибка двойного освобождения (double free) когда обе переменные будут пытаться освободить одну и ту же память в куче. А это потенциальная уязвимость в безопасности программы.

Для решения данной проблемы и обеспечения безопасности памяти после строки let s2 = s1; Rust считает s1 более недействительным, и этот механизм именуется семантикой перемещения (move semantics) или просто перемещением. Следовательно, нет необходимости в освобождении памяти s1 при выходе за пределы области видимости. При попытке использовать s1 после назначения его в s2 произойдёт ошибка:

fn main() {
    let s1 = String::from("hello");
    let s2 = s1;

    println!("{}, world!", s1); // ошибка, s1 больше не существует
}
         
Для владеющих другим языком программирования (Python, JS и т.д) данный механизм может чем-то напомнить поверхностное копирование, но это не одно и то же. В Rust первая переменная аннулируется. Вот как это выглядит:

Выбор такого подхода даёт дополнительное преимущество, а именно отсутствие автоматического создания "глубоких" копий. Следовательно, перемещение можно считать недорогим с точки зрения производительности во время выполнения.



Клонирование

Как быть, если всё таки понадобится копия для кучного типа? Для создания глубокой копии можно использовать общий метод clone:

fn main() {
    let s1 = String::from("hello"); // s1 = "hello"
    let s2 = s1.clone(); // s2 = "hello"

    println!("s1 = {s1}, s2 = {s2}"); // "s1 = hello, s2 = hello"
}

                  
Наилучшей практикой является избегать копий для кучных данных и предпочитать передачу владения или ссылки (урок 14.3), если это возможно. Это намного улучшит производительность программы.



Семантика копирования

Вернёмся к типам, размещающиеся на стеке:

fn main() {
    let x = 5; // x = 5
    let y = x; // y = 5

    println!("x = {x}, y = {y}"); // "x = 5, y = 5"
}
                  
Хмм, теперь кажется, что этот код противоречит тому, что мы только что узнали. Как без вызова clone переменная x всё ещё действительна и не была перемещена в y ? Дело в том, что их размер известен во время компиляции и строго фиксирован. И, как мы знаем, всё, что размещается на стеке, может быть быстро клонировано, а значит причин удалять переменную x нет.

В Rust есть специальная аннотация, называемая типажом (тема следующих частей 🤫) Copy, которую мы можем размещать на типах, хранящихся в стеке. Если тип реализует типаж Copy, переменные, которые его используют, не перемещаются, а тривиально копируются, что делает их действительными после присвоения другой переменной.

Только стековые типы могут реализовывать типаж Copy и никакие другие, которые требуют выделения памяти в куче или являются некоторой формой ресурсов. 