Clone

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна клонировать str1 в новую переменную str2 и вывести.

Напишите программу. Тестируется через stdin → stdout
Верно решили 155 учащихся
Из всех попыток 65% верных


fn main() {
    let str1 = String::from("🖼️ cloned string 🖼️");
    let str2 = str1.clone();
    println!("{str2}");
}

