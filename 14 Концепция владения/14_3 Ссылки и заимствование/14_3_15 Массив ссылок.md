Массив ссылок

В редакторе кода представлена функция refs_arr, которая вызывается с уже заполненным массивом. Реализовывать функцию main и вызывать refs_arr не требуется, это уже сделано, просто скрыто.

Дополните только refs_arr так, чтобы функция возвращала массив ссылок на элементы входного массива in_arr. Позиции ссылок в результирующем массиве должны соответствовать позициям соответствующих значений в in_arr.

Made by extrn!

Напишите программу. Тестируется через stdin → stdout
Верно решили 85 учащихся
Из всех попыток 36% верных


fn refs_arr(in_arr: &[i32; 10]) -> [&i32; 10] {
    let mut out_arr = [&0; 10];
    for i in 0..10 {
        out_arr[i] = &in_arr[i];
    }
    out_arr
}
