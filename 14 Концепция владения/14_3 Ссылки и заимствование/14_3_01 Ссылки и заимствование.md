Ссылки и заимствование

Ссылки
Передача по ссылке и заимствование
Вложенные ссылки
Изменяемые ссылки
Множественное заимствование
Ссылочный перебор коллекций
Копирование и перезаимствование
Висячие ссылки
Правила работы со ссылками



Ссылки

Ссылка (reference) - это скалярный тип данных, представленный адресом памяти, где хранится конкретный фрагмент информации (данных), будь то в стеке, куче или статической области.

Забегая вперёд, в Rust есть два типа указателей: безопасные указатели (ссылки) и сырые указатели, которые по своей сути небезопасны. Ссылка в Rust рассматривается как безопасный указатель на значение в памяти, благодаря системе типов и механизму владения, гарантирующие, что ссылки правильно управляются, не создают утечек памяти и не допускают ошибок при доступе к данным.

Для получения адреса используется соответствующий оператор - амперсанд &. Применение & к имени переменной дает адрес её значения и позволяет создавать ссылки на них:

fn main() {
    let x = 5;

    println!("{:p}", &x); // &x - адрес 0x...
}

                  
Игровая площадка ⛱️

Обратите внимание на спецификатор формата :p, использующийся для вывода адреса. Здесь следует сказать, что локальная переменная напрямую содержит значение, выделенное в памяти стека, поэтому адрес есть только у её значения. Глобальная же переменная не нуждается в ссылке, поскольку она всегда доступна из любого места.

Как и в случае с другими типами, можно создать переменную ссылочного типа. Для этого при аннотации амперсанд & должен предшествовать типу:

fn main() {
    let val_a: i32 = 5;

    let ref_a: &i32;
    ref_a = &val_a; // ref_a = 0x...
}

                  
При определении достаточно применить & и компилятор сам определит тип:

fn main() {
    let val_a: i32 = 5;

    let ref_a = &val_a; // ref_a = 0x...
}

                  
Не пытайтесь назначить адрес самостоятельно, поскольку за это отвечает операционная система. Поэтому адреса при каждом запуске программы могут отличаться. Исключением является низкоуровневая разработка: написание операционных систем, драйверов устройств, программного обеспечения для микроконтроллеров и т.д.

Вот как этот пример будет схематично выглядеть в памяти:

Обратите внимание, что ref_a также имеет адрес, как и любая другая переменная. Используя &ref_a, можно узнать адрес, по которому храниться 0x7ffd0ff3f61c, в данном случае это будет 0x7ffd0ff3f620.

Важным отличием от ссылок в других языках является то, что ссылки в Rust могут ссылаться только на данные, которые уже где-то хранятся и инициализированы:

fn main() {
    let val_a: i32;
    let ref_a = &val_a; // ошибка, получение ссылки на неинициализированное значение
}

                  
Ссылки строго типизированы и должны быть инициализированы ссылками того же типа. Попытка назначить ссылку, тип которого отличен, приведёт к ошибке:

fn main() {
    let val_a: i16 = 5;

    let ref_a: &i32 = &val_a; // ошибка, ожидалась ссылка &i32, получена &i16
}

                  
Кроме того, ссылка имеет фиксированный размер, зависящий от архитектуры платформы. Предположим, что в стеке есть две локальные переменные. Переменная val_a равна 5, а ref_a - это ссылка на val_a. Если архитектура 64 битная, то размер ref_a равен 8 байтам. 

fn main() {
    let val_a = 5;

    let ref_a = &val_a;

    println!("{}", std::mem::size_of_val(&ref_a)); // "8"
}

                  
Игровая площадка ⛱️

Для доступа к значению по ссылке необходимо выполнить операцию разыменования (dereference). Это делается с помощью соответствующего оператора разыменования *:

fn main() {
    let val_a = 5;

    let ref_a = &val_a;

    println!("{}", *ref_a); // "5"
}

                  
Игровая площадка ⛱️

Когда ссылка разыменовывается, мы получаем доступ к значению, на которое она указывает. В этом примере *ref_a разыменовывает ссылку ref_a, чтобы получить доступ к значению переменной val_a. Без разыменования ref_a будет просто ссылкой, а не значением переменной val_a.

Раз есть доступ к значению, мы можем проводить различные операции. Давайте сложим два значения, используя ссылки:

fn main() {
    let (val_a, val_b) = (5, 10);
    let (ref_a, ref_b) = (&val_a, &val_b); // Создаем ссылки на значения

    let sum = ref_a + ref_b; // Автоматическое разыменование ссылок при сложении
    println!("Результат сложения: {}", sum); // "Результат сложения: 15"
}

                  
Почему же здесь при сложении пропущено разыменование *a + *b? Rust автоматически выполняет разыменование при использовании некоторых операторов, а именно тех, кто реализует типаж Deref или DerefMut. Это делает работу со ссылками более удобной и интуитивно понятной. Некоторые из таких операторов включают в себя:

Подробнее

При выполнении математических операций предполагается, что целью являются значения по адресу памяти, а не сами указатели. Макрос вывода также поддерживает неявное разыменование:

fn main() {
    let val_a = 5;
    let ref_a = &val_a;

    println!("{}", ref_a); // "5"
}

          

Передача по ссылке и заимствование

Вместо перемещения или копирования переменной при передаче в функцию мы можем предоставить ссылку на неё, тем самым сэкономив память и увеличив производительность программы:

fn main() {
    let (val_a, val_b) = (5, 10);

    ref_sum(&val_a, &val_b);
}

fn ref_sum(a: &i32, b: &i32) {
    println!("a + b = {}", a + b); // "a + b = 15"
}

                  
Сигнатура функции использует & для индикации того, что тип параметра является ссылкой. Ещё одним важным аспектом ссылок в Rust является то, что они не владеют данными, на которые указывают. Это означает, что ссылки не отвечают за управление жизненным циклом данных, а лишь предоставляют доступ к ним. Для этого рассмотрим передачу String:

fn main() {
    let s1 = String::from("hello");

    print_str(&s1);

    let s2 = s1.clone(); // s1 все еще доступна
}

fn print_str(some_str: &String) {
    println!("some_str = {some_str}"); // "some_str = hello"

} // Здесь some_str выходит за пределы области видимости и удаляется

                  
При передаче s1 в функцию перемещения не происходит и владение остается у s1. Поэтому переменная не будет удалена, когда ссылка some_str выйдет из области видимости. Продемонстрируем эту концепцию на изображении:

Процесс создания ссылки именуется заимствованием (borrowing). Как и в реальной жизни, если человек чем-то владеет, мы можем это у него позаимствовать. По окончании работы мы должны вернуть это законному владельцу. Попытка изменить то, что было позаимствовано приведёт к ошибке:

fn main() {
    let val_a = 5;

    change(&val_a);
}

fn change(a: &i32) {
    *a = 100; // ошибка, изменение запрещено
}

                  
При работе со ссылками стоит быть внимательным. Срок жизни (тема следующих частей) владельца должно быть дольше срока заемщика (ссылки). Как обсуждалось выше, ссылка не владеет данными, и когда владелец удаляется, исчезает и значение, на которое ссылаются. Поэтому ссылка не может дальше использовать значение. Рассмотрим пример с правильным сроком жизни:

fn main() {
    let val_a = 5; // val_a - владелец

    {
        let ref_a = &val_a; // ref_a - заемщик 
    }

    println!("{}", val_a); 
}
      
Переменная val_a объявляется первой и является владельцем значения 5. Во внутреннем блоке ref_a ссылается на val_a и занимает значение 5. После закрытия внутреннего блока ref_a удаляется и val_a остается доступным. Владелец переживет заемщика и срок жизни корректен ! Теперь пример с неправильным сроком жизни:

fn main() {
    let ref_a: &i32; // ref_a - заемщик

    {
        let val_a = 5; // val_a = 5
        ref_a = &val_a; // ошибка, срока жизни val_a недостаточно для заема
    }

    println!("{}", ref_a);
}
          
Ссылка ref_a объявлена ​​во внешнем блоке, а переменная val_a ​во внутреннем и является владельцем значения 5. Далее ref_a ссылается на val_a и хочет занять значение 5. После выхода из внутреннего блока val_a удалиться вместе со своим значением. Но обратите внимание, так как срок жизни ref_a дольше, она продолжит заимствовать значение, что является ошибкой. Контролёр займа (borrow checker) не даст собрать программу, поскольку срок жизни ref_a превышает срок жизни владельца.



Вложенные ссылки

Ссылки сами по себе являются значениями и могут ссылаться на другие ссылки. В этом случае они именуются многоуровневыми (вложенными) ссылками. Вложенная ссылка - это ссылка, которая указывает на другую ссылку. Рассмотрим пример вложенных ссылок:

fn main() {
    let val_a = 5;
    let ref_a = &val_a;
    let ref_ref_a = &ref_a;
    let ref_ref_ref_a = &ref_ref_a;
}

                  
Cначала объявляем ссылку ref_a. Далее создаем ссылку на ref_a под названием ref_ref_a и еще одну ссылку ref_ref_ref_a, но уже на ref_ref_a. В памяти это будет выглядеть так:

Для просмотра адресов как самих ссылок, так и в значениях используйте {:p}, иначе макрос неявно разыменует их и выведется значение 5:

fn main() {
    let val_a = 5;
    let ref_a = &val_a;
    let ref_ref_a = &ref_a;
    let ref_ref_ref_a = &ref_ref_a;

    println!(
        "val_a's address = {:p},  val_a's volume = {}",
        &val_a, val_a
    );
    println!(
        "ref_a's address = {:p},  ref_a's volume = {:p}",
        &ref_a, ref_a
    );
    println!(
        "ref_ref_a's address = {:p},  ref_ref_a's volume = {:p}",
        &ref_ref_a, ref_ref_a
    );
    println!(
        "ref_ref_ref_a's address = {:p},  ref_ref_ref_a's volume = {:p}",
        &ref_ref_ref_a, ref_ref_ref_a
    );
}

                  
Игровая площадка ⛱️

Это всё можно было бы записать с помощью одной переменной, используя * и & 🫠:

fn main() {
    let ref_a: &&&i32 = &&&5;
    println!("5's address = {:p}", **ref_a);
    println!(
        "ref_a's address = {:p},  ref_a's volume = {:p}",
        &**ref_a, **ref_a
    );
    println!(
        "ref_ref_a's address = {:p},  ref_ref_a's volume = {:p}",
        &*ref_a, *ref_a
    );
    println!(
        "ref_ref_ref_a's address = {:p},  ref_ref_ref_a's volume = {:p}",
        &ref_a, ref_a
    );
}