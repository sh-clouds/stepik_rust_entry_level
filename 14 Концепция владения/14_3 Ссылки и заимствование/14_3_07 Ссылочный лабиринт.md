Ссылочный лабиринт 🌀

Напишите программу, где будет определена 5 уровневая ссылка на значение 5 и выведите адреса, по которым необходимо проследовать, чтобы получить итоговое значение. Вывод оформить в виде сообщений:

адрес -> адрес
адрес -> адрес
адрес -> адрес
адрес -> адрес
адрес -> адрес
адрес -> значение

                  
Напишите программу. Тестируется через stdin → stdout
Верно решили 109 учащихся
Из всех попыток 28% верных


fn main() {
    let ref_a: &&&&&i32 = &&&&&5;
    println!("{:p} -> {:p}", &ref_a, ref_a);
    println!("{:p} -> {:p}", &*ref_a, *ref_a);
    println!("{:p} -> {:p}", &**ref_a, **ref_a);
    println!("{:p} -> {:p}", &***ref_a, ***ref_a);
    println!("{:p} -> {:p}", &****ref_a, ****ref_a);
    println!("{:p} -> {}", &*****ref_a, *****ref_a);
}

