Владение и память

Владение
Виртуальная память
Программная память
Стек и куча
Статическая область данных
Правила владения
Область видимости



Владение

Почти все прошедшие темы не являются чем-то новым. Они так или иначе встречаются в других языках. И теперь мы переходим к действительно ключевой особенности Rust, а именно владение, обеспечивающее понимание всех остальных вещей, которые делают этот язык уникальным.

Владение - уникальная особенность Rust, оказывающая сильное влияние на остальную часть языка. Важность понимания данной темы заключается в том, что именно владение позволяет гарантировать безопасность памяти без необходимости использования сборщика мусора. Поэтому некоторым данный модуль может показаться немного сложным. Но уверяем вас это не сложнее, чем считать пользовательский ввод 😀.

Владение является частью модели памяти Rust и его безопасного управления, делая программный код более надежным, а разработчиков - более спокойными. Проблем, возникающих в области управления памятью предостаточно для того, чтобы не пренебрегать ими. Для понимания серьёзности уделения внимания безопасности памяти:

Исследование 2019 года показало, что 60-70% уязвимостей в iOS и macOS связаны с безопасностью памяти;
Около 70% всех уязвимостей в продуктах Microsoft, которые ежегодно устраняются с помощью обновлений безопасности, связаны с проблемами безопасности памяти;
По оценкам Google, 90% уязвимостей Android связаны с безопасностью памяти. Анализ 0-days, обнаруженных в ходе эксплуатации, показал, что более 80% эксплуатируемых уязвимостей были связаны с той же проблемой.
Владение (Ownership) это набор правил, определяющих, как программа на языке Rust управляет памятью. Все программы так или иначе должны управлять выделяемой им памятью. На сегодняшний день существуют разные средства, решающие эту задачу. Так, некоторые языки для этого используют сборщик мусора, в других же программист сам должен явно выделять и освобождать память. В Rust используется третий подход, где управление памятью происходит через систему владения с набором правил, которые проверяются компилятором, не замедляя при этом работу программы. При нарушении любого из правил программа не будет скомпилирована. Поскольку владение является новой концепцией, для некоторых программистов потребуется некоторое время, чтобы привыкнуть к ней.



Виртуальная память

При изучении некоторых других языков виртуальная и программная память обычно не рассматриваются на начальном уровне. Однако концепция управления памяти Rust привносит коррективы в траекторию обучения. Данные два раздела были включены исключительно для понимания темы стека и кучи, ну и владения, само собой. В рамках начального уровня достаточно пока поверхностного ознакомления.

Если помните, в начале урока Переменные и изменяемость было сказано, что программа хранит свои данные в ячейках памяти компьютера. Так вот, опуская детали, запуск программы происходит путем её загрузки в оперативную (основную) память компьютера. При этом данные программы по мере их создания хранятся там же. А как известно, всё, что храниться в ОЗУ после перезапуска компьютера стирается. Для постоянного же хранения программа просит операционную систему сохранить её данные на вторичном накопителе (HDD, SSD и т.д.).

Так как же получается, что при запуске программ операционная система не ограничивает нас работой только этой программы, позволяя запускать множество других ? Почему при выполнении множества задач одновременно компьютер сохраняет быстродействие ? Или как программы размером по сотни гигабайт умещаются в меньшую по объёму оперативную память ? Конечно же, не в последнюю очередь благодаря виртуальной памяти. Что же это такое ?

В вычислительной технике виртуальная память (virtual memory) - это метод управления памятью компьютера, который обеспечивает абстракцию ресурсов хранения, фактически (реально) доступных на данной машине, создавая иллюзию очень большой (неограниченной) оперативной памяти. 

Виртуальная память разбивается на небольшие блоки данных, называемые страницами. Страница, страница памяти или виртуальная страница - это непрерывный блок виртуальной памяти фиксированной длины, описываемый одной записью в таблице страниц. Это наименьшая единица данных для управления памятью в операционной системе, использующей виртуальную память.

Размер виртуальной страницы определяется архитектурой процессора и обычно составляет 4 килобайта. Однако большинство операционных систем позволяют программам определять размер страницы во время выполнения.

Операционная система, используя комбинацию аппаратного и программного обеспечения, отображает адреса памяти, используемые программой, именуемые виртуальными адресами, в физические адреса в памяти компьютера.

Виртуальные адреса программы напрямую связаны с виртуальными страницами. Когда программа обращается к виртуальному адресу, операционная система использует информацию из страничных таблиц для преобразования этого виртуального адреса в соответствующий физический адрес в оперативной памяти или на диске. Этот процесс называется трансляцией адресов или преобразованием виртуального адреса.

Оперативная память с точки зрения запущенной программы (процесса, задачи) выглядит как непрерывное адресное пространство или совокупность смежных сегментов. Схематично отображение процесса с её виртуальной памятью можно представить в виде:

Диапазон виртуальных адресов, доступных процессу, называется виртуальным адресным пространством процесса. Операционная система управляет виртуальными адресными пространствами и назначением реальной памяти виртуальной. Блок управления памятью (MMU) в центральном процессоре автоматически преобразует виртуальные адреса в физические. При запуске программ, размеры которых превышают реально доступную память в ОЗУ, виртуальное адресное пространство может быть расширено, используя неактивную память на вторичных накопителях с прямым доступом (HDD, SSD и т.д).

Каждый процесс пользовательского режима имеет своё собственное частное виртуальное адресное пространство.

32-битный процесс обычно имеет виртуальное адресное пространство в диапазоне 2 ГБ от 0x00000000 до 0x7FFFFFFF.

64-битный процесс в 64-битной версии Windows имеет виртуальное адресное пространство в диапазоне 128 терабайт от 0x00000000000 до 0x7FFFFFFFFFFF.

Диапазон виртуальных адресов иногда называют диапазоном виртуальной памяти. Схематично покажем некоторые ключевые особенности виртуальных адресных пространств:

На схеме показаны виртуальные адресные пространства для двух 64-битных процессов: Firefox.exe и Chrome.exe. Каждый процесс имеет своё собственное виртуальное адресное пространство в диапазоне от 0x0000000000 до 0x7FFFFFFFFFF. Каждый закрашенный блок представляет одну страницу размером 4 килобайта виртуальной или физической памяти. Процесс Firefox использует три смежные страницы виртуальных адресов, начиная с 0x7C902A73000. Однако эти три смежные страницы виртуальных адресов отображаются на несмежные страницы в физической памяти. Кроме того, оба процесса используют страницу виртуальной памяти, начинающуюся с 0x7C902A73000. Но обратите внимание, они отображаются на разные страницы физической памяти.



Программная память

Теперь, когда мы разобрались с виртуальной памятью и поняли, как операционная система управляет хранилищами компьютера, давайте перейдём к рассмотрению программной памяти. Программная память - это та область памяти, которая используется непосредственно нашими приложениями для выполнения различных задач. Если виртуальная память фокусируется на том, как операционная система абстрагирует физическую память, то программная память больше касается того, как программы структурируют и используют эту память.

В каждой программе используется несколько абстрактных сегментов памяти, таких как стек, куча, сегмент данных, сегмент кода и т.д. Эти сегменты позволяют программе эффективно хранить и обрабатывать данные, обеспечивая правильное функционирование и оптимальное использование выделенных ей ресурсов. Сегментацию программной памяти можно представить в виде:

В стандартной архитектуре x86 стек увеличивается к нулевому адресу. Это означает, что более новые (поздние) элементы, расположенные дальше в цепочке вызовов, находятся по численно меньшим адресам и ближе к куче. На некоторых других архитектурах он увеличивается в обратном направлении.



Стек и куча

Ошибочно полагать, что знание данной темы требуется только для Rust. Нет, стек и куча являются важнейшим компонентом программирования и на других языках. Владение будет описано через призму стека и кучи.

И стек, и куча являются абстрактными (виртуальными) сегментами памяти, которые доступны программе и используются во время её выполнения. Однако принципы их работы различны.

Стек (Stack) статически выделенная память, которая хранит данные в порядке их получения, а удаляет - в обратном. Этот механизм называется LIFO (последним пришёл, первым ушёл) что означает последний добавленный элемент удаляется первым:

Добавление данных именуется помещением в стек, а удаление - извлечением из стека. В рамках программы стек является приватной (частной) памятью, выделяемая каждому потоку. При запуске функции в стек добавляется стековый кадр, который состоит из состояния функции (локальных переменных, параметров и т.д.). Размер кадра стека определяется во время компиляции. Когда функция завершает работу, соответствующий кадр стека удаляется из памяти, и стек сжимается. В это время состояние функции, включая локальные переменные, автоматически удаляются из памяти. Все данные, хранящиеся в стеке, должны иметь фиксированный размер. Типы данных, размер которых может изменяться, должны храниться в куче.

Куча (Heap) динамически выделяемая память. По сравнению со стековой памятью, кучная память работает динамически, что простыми словами означает возможность использования типов данных с изменяемым размером. Также куча устроена менее организованно. Данные в ней не упорядочены по какому-либо определённому шаблону, как стек и могут быть доступны в произвольном порядке:

В рамках программы куча не является приватной и доступна из любого потока. Когда программа кладёт данные в кучу, ею запрашивается определённый объём пространства. Операционная система находит в куче свободный участок памяти достаточного размера, помечает его как используемый и возвращает указатель (тема следующих частей) являющийся адресом этого участка памяти. Этот процесс называется выделением памяти в куче и иногда сокращается до выделения памяти (помещение значений в стек не считается выделением). Когда программе понадобятся актуальные данные, ей придётся проследовать по указателю.

При этом сам указатель на участок памяти в куче имеет определённый фиксированный размер и может храниться в разных местах в зависимости от контекста:

На стеке: Если указатель создаётся и используется внутри функции, он будет храниться в стеке вместе с другими локальными переменными этой функции. Поэтому, когда указатель на стеке удаляется, соответствующая память кучи также должна быть освобождена. Обычно это делается с помощью функции деструктора или чего-то подобного. Этот паттерн освобождения ресурсов в конце времени жизни элемента иногда называется "Получение ресурса есть инициализация - Resource Acquisition Is Initialization (RAII)".

В куче: Если структура данных или объект, хранящий указатель, выделен в куче, сам указатель будет частью этой структуры данных в куче.

В статической памяти: Указатели, которые являются статическими или глобальными переменными, хранятся в статической области памяти.

Помещение в стек происходит гораздо быстрее, чем выделение памяти в куче, т.к. операционной системе не требуется искать место для размещения информации, потому как вверх стека всегда свободен. Также доступ к данным в куче происходит медленнее, чем в стеке, ввиду необходимости следования по адресу указателя. Современные процессоры работают быстрее, если они меньше перемещаются по памяти, обеспечивая лучшее выполнение своей работы, поскольку работа происходит с данными, которые находятся рядом, как в стеке, а не произвольно в случае с кучей.

Проблемы, решаемые владением:

Отслеживание использования данных участками программного кода;
Предотвращение исчерпания пространства путем минимизации количества дублирующихся данных в куче;
Очистка неиспользуемых данных.
Понимание владения устраняет слишком частый учёт особенностей памяти: стека и кучи. Подводя итог, основная цель владения - управление изменяемыми данными, т.е. данными кучи.



Статическая область данных

Данная тема выходит за рамки данного курса, и всё же дадим пару комментариев. Как вы могли заметить, помимо стека и кучи, есть другие сегменты программной памяти. Отдельно отметим статическую область данных (bss и data), где программа хранит статические (static) переменные, доступные на протяжении всего времени выполнения программы. Эти переменные обеспечивают доступ к своим значениям на протяжении всего времени выполнения программы без управления памятью, характерным для владения на стеке и кучи. Их нельзя переместить или удалить.



Правила владения

Для начала рассмотрим правила владения. Помните об этих правилах, пока мы работаем с примерами, которые их иллюстрируют:

У каждого значения в Rust есть владелец;
У значения в один момент времени может быть только один владелец;
Когда владелец покидает область видимости, значение удаляется.



Область видимости

При объявлении элемента программы, (например: функции или переменной) его имя может быть видимым (доступным) и использоваться в определённых частях программы. Контекст, в котором отображается имя называется его областью видимости. Другими словами, область видимости - это область внутри программы, в которой доступен какой-либо элемент.

fn main() {
    let num = 10; // num является владельцем значения 10
}

            

Локальная область

Так, переменная num, объявленная внутри функции main, отображается и доступна только в теле функции, которое задаётся соответствующим выражением блока {}. В этом случае переменная является локальной и имеет локальную область видимости, поскольку она создается внутри неё, т.е. локальна по отношению к функции.

Заметьте, что обращение к переменной возможно только с момента её объявления до конца текущей области видимости, т.е до закрывающей скобки }. Исключительно для примера воспользуемся выражением блока {} и создадим область видимости:

fn main() {

    {
                               // переменной num еще не существует
        let num = 10;          // num определена и может использоваться

        //num все еще доступна
    }
    let num_copy = num;        // ошибка, переменная num больше не существует
}

                  
В этом примере всё, что находиться внутри созданной нами области {}, при выходе из неё будет уничтожено, и переменная num будет удалена из стека, а память, выделенная для хранения значения 10, будет освобождена.



Глобальная область

Помимо локальной области, которую мы рассмотрели, существует также глобальная область, определяющая глобальные переменные, доступ к которым осуществляется из любой части программы. В глобальной области переменные определяются только как const либо static (тема следующих частей):

const DEGREE: i32 = 25; // определена в глобальной области

fn main() {
    const GRAVITY: f32 = 9.8; // определена в локальной области
}

                  
В отличие от локальных переменных, глобальные существуют в течение всего времени работы программы.

const DEGREE: i32 = 25;

fn main() {
    const GRAVITY: f32 = 9.8;

    // DEGREE и GRAVITY доступны для использования
}
// доступна только DEGREE

                  
Глобальные переменные имеют своё место, и, как говорят многие, определить, где и когда их использовать, может быть сложно. Поэтому их следует избегать и использовать только тогда, когда нет альтернатив.