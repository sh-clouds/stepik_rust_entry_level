Марсианин

Нам удалось установить контакт с внеземной цивилизацией через терминал связи. Ваша задача - написать программу, которая будет считывать сообщения (три строки каждая в отдельной переменной) из консоли и выводить их. Это позволит нам быстро анализировать информацию и установить эффективную коммуникацию с представителями инопланетной цивилизации.

Тестовые данные ✅

Sample Input:
Gral!ndark!
Sovhr! kvralodenom?
Zvtyrn!zhonelii!

Sample Outpu
Gral!ndark!
Sovhr! kvralodenom?
Zvtyrn!zhonelii!

Напишите программу. Тестируется через stdin → stdout
Верно решили 562 учащихся
Из всех попыток 56% верных


use std::io;

fn main() {
    // put your Rust code here
    let mut user_input = String::new();

    io::stdin()
        .read_line(&mut user_input) 
        .expect("Не удалось прочитать ввод"); 
    
    io::stdin()    
        .read_line(&mut user_input)  
        .expect("Не удалось прочитать ввод"); 
    
    io::stdin()      
        .read_line(&mut user_input)  
        .expect("Не удалось прочитать ввод");   
    
    println!("{}", user_input.to_string());   
}

