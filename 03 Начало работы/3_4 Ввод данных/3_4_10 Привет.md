Привет!

Напишите программу, которая считывает имя и затем выводит приветственное сообщение с использованием этого имени:

Привет, {имя}!
Тестовые данные ✅

Sample Input:
Грэйдон

Sample Output:
Привет, Грэйдон!

Напишите программу. Тестируется через stdin → stdout
Верно решили 587 учащихся
Из всех попыток 28% верных


use std::io;

fn main() {
    // put your Rust code here
    let mut user_input = String::new();
    io::stdin()
        .read_line(&mut user_input)        
        .expect("Не удалось прочитать ввод"); 
    
    println!("Привет, {}!", user_input.trim());
}
