Вещественный интервал (a, b)

Напишите программу, которая считывает три вещественных числа (x, a и b) и выводит (до 1 знака), принадлежит ли значение x интервалу (a, b).

Если принадлежит вывести: Значение {x} принадлежит интервалу ({a}, {b}).
Иначе вывести: Значение {x} не принадлежит интервалу ({a}, {b}).

Тестовые данные ✅

Sample Input:
-3
-5
-1

Sample Output:
Значение -3.0 принадлежит интервалу (-5.0, -1.0)

Напишите программу. Тестируется через stdin → stdout
Верно решили 77 учащихся
Из всех попыток 41% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {    
    let mut i1: f64 = input();
    let mut i2: f64 = input();   
    let mut i3: f64 = input();     

    if i1>i2 && i1<i3 
    {
        println!("Значение {:.1} принадлежит интервалу ({:.1}, {:.1})", i1, i2, i3);        
    }
    else
    {
        println!("Значение {:.1} не принадлежит интервалу ({:.1}, {:.1})", i1, i2, i3);      
    }     
}

