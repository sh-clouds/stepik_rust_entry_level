Какой из методов можно использовать для проверки, что строка состоит только из цифр?

Выберите один вариант из списка
Верно решили 117 учащихся
Из всех попыток 85% верных
 Верно. Так держать!

input.trim().chars().filter(char::is_numeric)

input.trim().chars().any(char::is_numeric)

input.trim().chars().all(char::is_numeric) +

input.trim().chars().none(char::is_numeric)