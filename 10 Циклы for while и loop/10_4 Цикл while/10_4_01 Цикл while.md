Цикл while

Следующая важная циклическая конструкция while, известная как цикл с условием, содержащий только проверочное условие и тело:

while условное_выражение {
    // тело
}

Условие проверяется до выполнения тела, что означает, если при первой проверке условие ложно, цикл будет полностью пропущен. В противном случае цикл выполняется пока условное выражение истинно true, как только условие становится false цикл завершается. Рассмотрим пример:

fn main() {
    let mut num = 3;

    while num != 0 {
        println!("{num}");
        num -= 1;
    }
}

Сначала проверяется условие, т.к. условие 3 != 0 истинно, будет выполнено тело цикла. Каждый раз, когда выполняется тело while, значение переменной num уменьшается на 1 до тех пор пока num не станет равной 0. В этом случае условное выражение 0 != 0 вернёт false и цикл завершиться.

Игровая площадка ⛱️



Бесконечный while

Важно, чтобы условие в какой-то момент стало false, иначе получиться бесконечный цикл (зацикливание):

fn main() {
    let mut num = 3;

    while num != 0 {
        println!("{num}!");
    }
}

Так, если бы мы забыли написать строчку num -= 1;, как в этом примере получился бы бесконечный цикл. Всегда проверяйте условие на завершение!

Раз есть условие можно применять составные выражения:

fn main() {
    let (a, b, mut count) = (3, 6, 0);

    while count < a && count < b {
        println!("count: {count}, a: {a}, b: {b}");
        count += 1
    }
}

Игровая площадка ⛱️



Бесконечный пользовательский ввод

Традиционно для "бесконечного" считывания пользовательского ввода с консоли используется цикл while. В отличие от for, дабы избежать зацикливания, while должен каким-то образом узнать, когда ему следует остановиться. Одно из решений заключается в использовании некоторого специального символа, иногда называемого сигнальным символом в качестве сигнала останова. Такой символ может быть любым, что не используется в обычных вводимых данных, например, строка exit, символ # или символ новой строки \n и т.п:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);
    }
}

При сравнении не забываем, что в input содержатся специальные символы, которые необходимо убирать. Очистка input была вынесена перед считыванием, поскольку её значение нам ещё пригодиться в условном выражении 🥲.

Отдельного рассмотрения требует преобразование input в число. Простого добавления того, что мы делали раньше недостаточно, так как ввод сигнального символа # с последующей его конвертации в число приведёт к панике:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        // если input = "#" то будет ошибка
        let number: i32 = input.trim().parse().expect("Ожидалось целое число.");
        println!("Вы ввели число: {number}");
    }
}

Можно, конечно, вместо символа # сравнивать с "числом" но тогда мы не сможем его использовать. Решением будет воспользоваться либо выражением match, либо поместить конвертацию в условие, тем самым гарантируя, что input содержит числовое значение. Подробное изучение match и элементов второго метода оставим на следующую часть и рассмотрим только их применение.



Выражение match

Начнём с match:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        let number: i32 = match input.trim().parse() {
            Ok(num) => num,
            Err(_) => continue,
        };

        println!("Вы ввели число: {number}");
    }
}

Игровая площадка ⛱️

В данном примере мы заменяем вызов expect на выражение match, обрабатывая результат parse. Вспомним, что parse возвращает перечисление Result, которое имеет варианты Ok и Err.

Если parse успешно преобразует строку в число, он вернёт вариант Ok, содержащий преобразованное число. В противном случае будет возвращено значение Err, содержащий более подробную информацию об ошибке. В зависимости от варианта match сравнит результат parse и выполнит ветку Ok или Err соответственно.

Ветки match состоят из двух частей: шаблона и некоторого кода. Здесь первая ветка имеет шаблон, который является значением Ok, затем идёт оператор =>, разделяющий шаблон и код для выполнения. Код в этом случае - это просто выражение, представленное значением, содержащимся в num. Каждая ветка отделяется от последующей при помощи запятой.

При выполнении ветки Ok(num) => num переменная num привязывается к значению, содержащемуся внутри Ok, присваивая num конвертированное числовое значение. После => выполняется код данной ветки, возвращая значение num и инициализируя им переменную number.

Что же касается ветки Err(_) => continue,. Знак нижнего подчеркивания _ является всеохватывающим выражением, обрабатывая совпадение всех значений Err, независимо от того, какая информация находится внутри. Выполнение кода второй ветки continue позволит пропустить оставшуюся часть текущей итерации цикла и перейти к следующей итерации. Так мы сможем эффективно игнорировать все ошибки, с которыми parse может столкнуться! Тема continue рассматривается на следующем уроке.



Конвертация в условии

Со вторым вариантом тоже не так просто, но и ничего сложного. Для гарантии того, что в input содержится только числовое значение, необходимо посимвольно проверить его содержимое на соответствие числу. Создадим флаг is_number, по умолчанию равный true:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        let mut is_number = true;
    }
}

Теперь нам нужно пройтись по всем символам, содержащимся в input. Воспользуемся циклом for, передав в качестве перебираемого значения итератор на input:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        let mut is_number = true;
        for ch in input.trim().chars() {}
    }
}

Метод chars создаёт итератор по символам строки. К примеру, если input будет содержать 12a\n, то input.trim().chars() создаст итератор, который вернёт 1, 2 и a.

Теперь в ch будут все символы input. Добавим проверку, что сh является числовым значением, вызвав соответствующий метод is_numeric:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        let mut is_number = true;
        for ch in input.trim().chars() {
            if ch.is_numeric() == false {
                is_number = false;
            }
        }
    }
}

is_numeric возвращает true, если проверяемый символ относится к одной из общих категорий чисел. Обратите внимание на переменную is_number его значение будет равно true, только если все символы в input являются цифрами. Осталось проверить is_number и реализовать конвертацию:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        let mut is_number = true;
        for ch in input.trim().chars() {
            if ch.is_numeric() == false {
                is_number = false;
            }
        }

        if is_number {
            let num: i32 = input.trim().parse().expect("Ожидалось целое число");
            println!("Вы ввели число: {}", num);
        } else {
            println!(
                "Ошибка! Введенная строка {} содержит не только цифры",
                input.trim()
            );
        }
    }
}

Игровая площадка ⛱️

В принципе всё. Конечно, это всё можно было бы написать короче:

fn main() {
    let mut input = String::new();

    while input.trim() != "#" {
        input.clear();
        std::io::stdin()
            .read_line(&mut input)
            .expect("Ошибка при чтении ввода.");
        println!("Содержимое input: {:#?}", input);

        if input.trim().chars().all(char::is_numeric) {
            let num: i32 = input.trim().parse().expect("Ожидалось целое число");
            println!("Вы ввели число: {}", num);
        } else {
            println!(
                "Ошибка! Введенная строка {} содержит не только цифры",
                input.trim()
            );
        }
    }
}

Игровая площадка ⛱️

Метод all применяет переданную ассоциированную функцию к каждому элементу итератора и возвращает true, если переданная ассоциированная функция возвращает true для всех элементов, иначе возвращает false. В данном случае в all передаётся ранее использованный char::is_numeric которая проверяет, является ли символ числом.

🧡 Что хотелось бы сказать 🙃. Используйте тот способ, который для вас показался самым простым, пока не пройдёте все темы, охватывающие представленные варианты. Ну и да, идиоматическим решением является пример с match.

