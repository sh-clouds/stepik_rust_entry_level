Перебор коллекций

Перейдём к рассмотрению более идиоматического способа перебора коллекций. for позволяет передать в качестве перебираемого значения однотипные коллекции. Возможно, за это ему и отдают предпочтение. Давайте создадим массив и выведем его значения:

fn main() {
    let array = [0, 1, 1, 2, 3, 5, 8, 13, 21];

    for iter in array {
        println!("{iter}");
    }
}

Работа, совершаемая циклом, аналогична диапазону, только теперь iter будет проходиться непосредственно по содержимому коллекции.

Почему же нельзя перебирать мультитипные коллекции (например, кортеж)? Отвечая коротко. На каждой итерации цикла элемент кортежа может иметь другой тип, что характерно для динамически типизированных языков (Python и т.д.). Хотя есть прецедент такой возможности в статически типизированном языке D, но это уже другая история. Может, когда-нибудь такая возможность будет добавлена, тем не менее необходимости в этом нет. Существуют куда более удобные средства объединения разнотипных данных, а это уже тема следующей части 😉.