Индексный перебор

Многим, возможно, пришла идея, а не использовать ли переменную цикла для получения элементов массива по индексу. Вспомним из предыдущего урока пример увеличения элементов массива на какое-то число:

fn main() {
    let mut sum = [0; 100];
    sum[0] += 1;
    sum[1] += 1;
    sum[2] += 1;
    //..........
    sum[99] += 1;
    println!("{:?}",sum);
}

Пришло время переложить и эту работу на циклы. Приступим! Зная, что элементов в массиве 100, необходимо сделать диапазон от 0 до 99:

fn main() {
    let mut array = [0; 100];

    for item in 0..=99 {}
}

Теперь, когда item перебирает нужный диапазон, реализуем индексирование как sum[0], sum[1], ... :

fn main() {
    let mut array = [0; 100];

    for item in 0..=99 {
        array[item];
    }
}


Помните, попытка получения доступа к элементу массива, находящемуся за его пределами, приведёт к ошибке.
 

Затем, после получения значения из массива, добавим увеличение array[item] += 1:

fn main() {
    let mut array = [0; 100];

    for item in 0..=99 {
        array[item] += 1;
    }
}

Последний штрих 🍒, вывод получившегося массива:

fn main() {
    let mut array = [0; 100];

    for item in 0..=99 {
        array[item] += 1;
    }
    println!("{:?}",array);
}

Игровая площадка ⛱️
Можно запускать!

Вся работоспособность программы держится на знании того, что диапазон ограничен числом 99. Добавим в цикл условие, которое не допустит обращения к несуществующему элементу и выходу за пределы массива:

fn main() {
    let mut array = [0; 100];

    for item in  0..=150 {
        if item <= 99 {
            array[item] += 1;
        }
    }
    println!("{:?}",array);
}

Теперь, каким бы не был конец диапазона, выхода за пределы массива не будет! Перейдем к более правильной обработке данной ситуации.



Получение длины массива

Когда длина коллекции заранее неизвестна, необходимо вызвать метод возврата размера. Укажем вместо 99 длину массива, убрав включение =, т.к. может произойти выход за пределы. Для получения длины необходимо у переменной array с помощью оператора доступа к элементам . вызвать соответствующий метод len():

fn main() {
    let mut array = [0; 100];

    for item in 0..array.len() { // 0..100 
        array[item] += 1;
    }
    println!("{:?}",array);
}
Теперь в случае изменения длины массива нам не придётся каждый раз менять конец диапазона.

Используйте метод len, даже если длина коллекции известна заранее! Не указывайте длину явно!
