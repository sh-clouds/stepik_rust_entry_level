Квадрат диапазона

Напишите программу, которая считывает два целых числа a и b, а затем выводит квадрат всех целых чисел, расположенных между a и b (включая их самих), как показано в примерах.

Гарантируется, что a≠b!

Тестовые данные ✅

Sample Input 1:
-10
-5

Sample Output 1:
100
81
64
49
36
25

Sample Input 2:
-10
-20

Sample Output 2:
400
361
324
289
256
225
196
169
144
121
100

Напишите программу. Тестируется через stdin → stdout
Верно решили 74 учащихся
Из всех попыток 75% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {            
    let mut a: i32 = input();  
    let mut b: i32 = input(); 
    let mut temp: i32 = 0;

    if a > b
    {
        temp = a;
        a = b;
        b = temp;
    }   
    
    for i in a..=b {
       println!("{}", i*i); 
    }    
}