Циклический вывод 2

Напишите программу, которая считывает натуральное число n (u8) и столько же вводимых пользователем значений, а затем выводит их, как показано в примерах.

Тестовые данные ✅

Sample Input 1:
1
1

Sample Output 1:
Вы ввели: 1


Sample Input 2:
2
-1.5
-2.7

Sample Output 2:
Вы ввели: -1.5
Вы ввели: -2.7

Напишите программу. Тестируется через stdin → stdout
Верно решили 82 учащихся
Из всех попыток 72% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {  
    let mut n: u8 = input();  

    for i in 0..n {
        let mut s: String = input();   
        println!("Вы ввели: {s}");
        
        //if i%1 == 0
        //{
        //    println!("Вы ввели: {:.1}", i);  
        //}
        //else
        //{
        //    println!("Вы ввели: {:.1}", i as f64);
        //}        
    }      
}
