Четный диапазон

Напишите программу, которая считывает три целых числа start (i32), end (i32) и step (usize) и выводит для диапазона start..=end с шагом step все четные числа.

Тестовые данные ✅
Sample Input:
-20
0
2

Sample Output:
-20
-18
-16
-14
-12
-10
-8
-6
-4
-2
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 77 учащихся
Из всех попыток 79% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {  
    let mut start: i32 = input();     
    let mut end: i32 = input(); 
    let mut step: usize = input();     
    let mut multy: f64 = 1.0;
    
    for i in (start..= end).step_by(step) {
        if i%2 == 0
        {
            println!("{i:.1}");
        }             
    }        
}

