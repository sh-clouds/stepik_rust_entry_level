Делитель step

Напишите программу, которая считывает три целых числа start (i32), end (i32) и step (usize) и выводит все значения диапазона start..=end с шагом step, для которых делителем является step.

Тестовые данные ✅

Sample Input:
0
15
3

Sample Output:
0
3
6
9
12
15

Напишите программу. Тестируется через stdin → stdout
Верно решили 74 учащихся
Из всех попыток 78% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {  
    let mut start: i32 = input();     
    let mut end: i32 = input(); 
    let mut step: usize = input();     
    let mut multy: f64 = 1.0;
    
    for i in (start..= end).step_by(step) {
        if i % step as i32 == 0
        {
            println!("{i:.1}");
        }             
    }        
}

