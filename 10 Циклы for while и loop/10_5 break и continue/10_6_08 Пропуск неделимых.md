Пропуск неделимых

Напишите программу, которая считывает два натуральных (u32) числа d и n, и n вводимых пользователем целых значений, а затем выводит среди них те, для которых d является делителем.

Тестовые данные ✅

Sample Input:
2
9
1
2
3
4
5
6
7
8
9

Sample Output:
2
4
6
8

Напишите программу. Тестируется через stdin → stdout
Верно решили 52 учащихся
Из всех попыток 68% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let mut d: u32 = input();
    let mut n: u32 = input();
    let mut a: i32;  

    for i in 0..n {
        a = input();
        if a % d as i32 == 0
        {
            println!("{a}");
        }  
    }            
}


