Сортировка 🥚

Напишите программу, которая считывает целые числа до ввода нуля и выводит сумму отрицательных и положительных значений.

Тестовые данные ✅

Sample Input:
-5
-4
-3
-2
-1
1
2
3
4
5
0

Sample Output:
Сумма отрицательных чисел: -15
Сумма положительных чисел: 15


Напишите программу. Тестируется через stdin → stdout
Верно решили 56 учащихся
Из всех попыток 95% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let mut sum_pos: i32 = 0;
    let mut sum_neg: i32 = 0;
    let mut a: i32;
    loop
    {
        a = input();
        if a > 0
        {
            sum_pos += a;
        }  
        else if a < 0
        {
            sum_neg += a;
        }  
        else
        {                        
            break;
        } 
    }            
    println!("Сумма отрицательных чисел: {sum_neg}"); 
    println!("Сумма положительных чисел: {sum_pos}");  
}

