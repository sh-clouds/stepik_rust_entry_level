Метки циклов

В некоторых языках break и continue ограничиваются циклом, в котором используются. В ситуациях с вложенными циклами, когда нам нужно указать, к какому из них относится break и continue приходиться искать обходные решения. Разработчики Rust поразмыслив предложили нечто интересное, а именно метки циклов (loop labels). Метки обеспечивают гибкость и расширяют область действия инструкций break и continue, позволяя регулировать поведение циклов. 

Объявление метки очень похоже на аннотацию типа и выглядит для всех трёх циклов одинаково:

'label: for
'label: while
'label: loop

Далее метка должна быть передана инструкции break или continue:

break 'label;
continue 'label;

Начнём сразу с вложенных циклов:

fn main() {
    let mut counter = 0;

    // Внешний цикл с меткой 'outer_loop
    'outer_loop: loop {
        println!("Внешний цикл, счетчик = {}", counter);

        let mut inner_counter = 10;

        // Внутренний цикл с меткой 'inner_loop
        'inner_loop: loop {
            println!("  Внутренний цикл, внутренний счетчик = {}", inner_counter);

            // Условие для выхода из внутреннего цикла
            if inner_counter == 9 {
                println!("  Выход из внутреннего цикла");
                break 'inner_loop;
            }
            inner_counter -= 1;
        }

        // Условие для выхода из внешнего цикла
        if counter == 3 {
            println!("Выход из внешнего цикла");
            break 'outer_loop;
        }
        counter += 1;
    }
}

Игровая площадка ⛱️

Внешний цикл с меткой 'outer_loop начинает выполнение с counter = 0, для каждой итерации внешнего цикла запускается внутренний цикл с inner_counter = 10. Внутренний цикл выполняется, уменьшая inner_counter до тех пор, пока не достигнет 9, после чего завершается. Внешний цикл увеличивает counter на 1 и проверяет условие выхода. Это продолжается до тех пор, пока counter не достигнет 3, после чего внешний цикл завершается.

Метки могут быть использованы в циклах любой вложенности, в рамках разумного конечно 😅:

fn main() {
    'outer: loop {
        println!("Вошли во внешний цикл");

        'inner1: loop {
            println!("Вошли в 1-й внутренний цикл");

            'inner2: loop {
                println!("Вошли во 2-й внутренний цикл");

                'inner3: loop {
                    println!("Вошли в 3-й внутренний цикл");
        
                    break 'outer;
                }
            }
        }
    }

    println!("Вышли из внешнего цикла");
}

Игровая площадка ⛱️


Считается, что использование необычных языковых инструментов, таких как метки циклов обычно считается плохой практикой, уменьшающий понятность и читаемость кода.
