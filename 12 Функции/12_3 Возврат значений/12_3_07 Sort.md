Sort 📉

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считать в массив arr 10 целых чисел и строку ord, задающую порядок сортировки:

asc - сортировка по возрастанию;
dec - сортировка по убыванию;
После вывести отсортированную коллекцию с помощью заполнителя {:?}.

Тестовые данные ✅

Sample Input:
8
9
-1
0
10
-8
-2
7
-7
-3
asc

Sample Output:
[-8, -7, -3, -2, -1, 0, 7, 8, 9, 10]

Напишите программу. Тестируется через stdin → stdout
Верно решил 41 учащийся
Из всех попыток 55% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}


fn main() {
    let mut arr = [0_i32; 10];

    for i in 0..10  
    {       
        arr[i] =  input();
    }  
    
    let mut s: String = input();    
    arr = sort(arr, &s);    
    println!("{:?}", arr);
}

fn sort(mut arr: [i32; 10], ord: &str) -> [i32; 10] {
    let mut d: i32;
    let mut f: bool = false;
    let mut p: usize = 0; //pointer b          
    
    if ord == "asc"
    {
        let mut a = [10000_i32; 10];
        let mut b = [10000_i32; 12]; //temp          
        
        for j in 0..10  
        {       
            for i in 0..10
            {
                a[i] = b[i];
            }

            d =  arr[j];
            p = 0;
            f = false;

            for i in 0..10
            {
                if (d < a[i]) && (f == false) 
                {
                    f = true;
                    b[p] = d;                
                    p += 1;
                    b[p] = a[i];
                }
                else
                {
                    b[p] = a[i];                
                }
                p += 1;             
            }
        }
        for i in 0..10
        {
            a[i] = b[i];
        }
        a        
    }
    else
    {
        let mut a = [-10000_i32; 10];
        let mut b = [-10000_i32; 12]; //temp
        
        for j in 0..10  
        {       
            for i in 0..10
            {
                a[i] = b[i];
            }

            d =  arr[j];
            p = 0;
            f = false;

            for i in 0..10
            {
                if (d > a[i]) && (f == false) 
                {
                    f = true;
                    b[p] = d;                
                    p += 1;
                    b[p] = a[i];
                }
                else
                {
                    b[p] = a[i];                
                }
                p += 1;             
            }
        } 
        for i in 0..10
        {
            a[i] = b[i];
        }
        a        
    } 
}



