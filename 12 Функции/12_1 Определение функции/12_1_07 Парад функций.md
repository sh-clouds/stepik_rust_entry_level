Парад функций 🪐

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считывать в функцию func4() строку и выводить ее. Редактировать можно только функции func4() и start().

Тестовые данные ✅

Sample Input:
Строка прибывшая из далекой четвертой функции 🪐

Sample Output:
Строка прибывшая из далекой четвертой функции 🪐

Напишите программу. Тестируется через stdin → stdout
Верно решили 55 учащихся
Из всех попыток 81% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}


fn func2() {
    func3();
}

fn func1() {
    func2();
}

fn start() {
   func1()     
}

fn main() {
    start();
}

fn func3() {
    func4();
}
fn func4() {
    // Реализовать считывание и вывод строки
    let s: String = input();
    println!("{s}")
}

