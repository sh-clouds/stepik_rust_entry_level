Множественный вызов 📞

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считывать натуральное число n (u32) и столько же раз вызывать функцию print_str() из main() при этом выводя номер вызова:

1) была вызвана функция!
2) была вызвана функция!
3) была вызвана функция!
........................
Редактировать можно только функцию main()!

Тестовые данные ✅

Sample Input 1:
1

Sample Output 1:
1) была вызвана функция!


Sample Input 2:
2

Sample Output 2:
1) была вызвана функция!
2) была вызвана функция!


Напишите программу. Тестируется через stdin → stdout
Верно решил 61 учащийся
Из всех попыток 72% верных


use std::io;
use std::str::FromStr;

fn print_str() {
    print!("была вызвана функция!");
}

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let n: u32 = input();
    
    for i in 1..=n 
    {    
        print!("{i}) ");        
        print_str();
        println!("");           
    }      
}

