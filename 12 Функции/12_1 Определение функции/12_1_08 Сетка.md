Сетка #️⃣

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считывать два натуральных (u8) числа M и N, а затем выводить сетку M×N. Каждая ячейка сетки должна иметь ширину 3×2 символа.

Редактировать можно только функцию print_grid()!

Так, для M = 3 и N = 5 результатом будет:

+--+--+--+--+--+
|  |  |  |  |  |
+--+--+--+--+--+
|  |  |  |  |  |
+--+--+--+--+--+
|  |  |  |  |  |
+--+--+--+--+--+

Тестовые данные ✅

Sample Input:
1
1

Sample Output:
+--+
|  |
+--+

Напишите программу. Тестируется через stdin → stdout
Верно решили 47 учащихся
Из всех попыток 74% верных



use std::io;
use std::str::FromStr;

fn print_str() {
    print!("была вызвана функция!");
}

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn print_pmmm(){
    print!("+--");
}
fn print_p(){
    println!("+");
}
fn print_vss(){
    print!("|  ");
}
fn print_v(){
    println!("|");
}
fn main() {
    print_grid();
}

fn print_grid() {
    let mut m: u8 = input();
    let mut n: u8 = input();

    //m = 1;
    //n = 2;
    
    for i in 1..=m 
    { 
        for j in 1..=n 
        {             
            print_pmmm();                     
        }
        print_p();  
        
        for j in 1..=n 
        {              
            print_vss();          
        } 
        print_v();                
    }
    
        for j in 1..=n 
        {             
            print_pmmm();                     
        }
        print_p();     
}

