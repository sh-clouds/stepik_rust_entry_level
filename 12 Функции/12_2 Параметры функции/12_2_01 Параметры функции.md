Параметры функции

Может возникнуть ситуация, когда необходимо передать данные в функцию. В этом случае мы можем определить функцию с параметрами. Параметр - это специальная переменная, являющаяся частью сигнатуры функции. Сигнатура функции определяет общую информацию о функции: имя, параметры и возвращаемое значение / результат (урок 12.3).

Параметры объявляются внутри круглых скобок () отделяясь друг от друга запятой, при этом количество параметров не должно превышать 65535 🫣:

fn function_name(par1: u8, par2: i16, par3: f32, par4: char) {
    /*
    Тело функции
    */
}

В идеальном случае количество параметров функции должно быть равно нулю, допустимо один или два, три в крайних случаях, четыре и более - никогда!

Также обязательным является аннотация типа для параметров. Это намеренное решение в дизайне Rust, позволяющее:

Определить компилятору нужный тип, исключая дальнейшие неясности при работе с ними;
Выдача компилятором более информативных сообщений об ошибках.
Определим функцию sum с двумя параметрами a и b, и выведем их сумму:

fn sum(a: i32, b: i32) {
    println!("a + b = {}", a + b);
}

fn main() {
    
}

После определения функции в точке вызова ей необходимо предоставить конкретные значения этих параметров. Конкретные значения именуются аргументами, однако всё чаще слова параметр и аргумент используют как синонимы либо для переменных в определении функции, либо для конкретных значений, передаваемых при вызове. Для ясности мы не будем их использавать как синонимы.

Вызовем sum, передав в качестве аргументов числа 5 и 10.

// a = 5, b = 10
fn sum(a: i32, b: i32) {
    println!("a + b = {}", a + b); // "a + b = 15"
}

fn main() {
    sum(5, 10);
}

Игровая площадка ⛱️

Параметры инициализируются значениями предоставленных аргументов, т.е. параметр a инициализируется значением 5, а b значением 10.


Важно отметить, что параметры ограничены областью видимости функции. Это значит, что они доступны исключительно внутри той функции, в которой объявлены.



Изменяемые параметры

Поскольку параметры - это переменные, то по умолчанию они неизменяемы, при попытке их изменить мы получим ошибку:

fn sum(a: i32, b: i32) {
    a = 100; // ошибка!

    println!("a + b = {}", a + b);
}

fn main() {
    sum(5, 10);
}
Если хотите сделать параметр изменяемым, необходимо перед его именем указать mut.

fn sum(mut a: i32, b: i32) {
    a = 100;

    println!("a + b = {}", a + b); // "a + b = 110"
}

fn main() {
    sum(5, 10);
}

Вместо литералов в качестве аргументов могут быть переменные:

// a = 5, b = 10
fn sum(a: i32, b: i32) {
    println!("a + b = {}", a + b); // "a + b = 15"
}

fn main() {
    let (arg1, arg2) = (5, 10);
    sum(arg1, arg2);
}

В этом случае значение переменной, ровно как и литерал копируется в параметр. Этот процесс именуется передачей по значению, т.е. при вызове функции sum создаются новые переменные типа i32 по имени a и b, инициализирующиеся значениями переменных arg1 и arg2 соответственно. Это обеспечивает изоляцию данных, позволяя sum работать с копиями arg1 и arg2.

Для наглядности продемонстрируем, что изменения внутри sum не повлияют на arg1 и arg2:

fn sum(mut a: i32, mut b: i32) {
    a = 100;
    b = 200;

    println!("a = {a}, b = {b}"); // "a = 100, b = 200"
}

fn main() {
    let (arg1, arg2) = (5, 10);

    sum(arg1, arg2);

    println!("arg1 = {arg1}, arg2 = {arg2}"); // "arg1 = 5, arg2 = 10"
}

За процесс передачи, одним из которых является передача по значению отвечает концепция владения (модуль 14).

Игровая площадка ⛱️



Передача коллекций

В качестве параметров могут быть и другие типы, например, составные. Рассмотрим передачу кортежа, для этого достаточно аннотировать тип tup: (i32, i32). Поместим числа 5 и 10 в кортеж и передадим его в функцию:

fn sum(tup: (i32, i32)) {
    println!("{0} + {1} = {2}", tup.0, tup.1, tup.0 + tup.1); // "5 + 10 = 15"
}

fn main() {
    sum((5, 10));
}
В передаче массива также нет ничего сложного. При аннотации типа в квадратных скобках указывается тип элементов массива, а затем через точку с запятой ; их количество:

fn sum(arr: [i32; 2]) {
    println!("{0} + {1} = {2}", arr[0], arr[1], arr[0] + arr[1]); // "5 + 10 = 15"
}

fn main() {
    sum([5,10]);
}
Аналогично, если необходимо изменить элемент коллекции используйте mut: 

fn sum(mut tuple: (i32, i32), mut arr: [i32; 2]) {
    tuple.0 = 100;
    arr[1] = 200;

    println!("{0} + {1} = {2}", tuple.0, arr[1], tuple.0 + arr[1]); // "100 + 200 = 300"
}

fn main() {
    sum((5,10), [15,20]);
}

