Итоговые баллы 📝

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считать первичные баллы (u8) единого государственного экзамена по информатике и выводить тестовые баллы.

Редактировать можно только функции get_primary_sc() и print_test_sc() !

Тестовые данные ✅

Sample Input:
1

Sample Output:
7

Напишите программу. Тестируется через stdin → stdout
Верно решили 46 учащихся
Из всех попыток 73% верных


use std::io;

fn main() {
    get_primary_sc();
}

fn get_primary_sc() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    let primary_sc = input.trim().parse::<u8>().unwrap();
    
    print_test_sc(primary_sc);
}

fn print_test_sc(scores: u8) {
    let arr = [0, 7, 14, 20, 27, 34, 40, 
        43, 46, 48, 51, 54, 56, 59, 62, 64,
        67, 70, 72, 75, 78, 80, 83, 85, 88,
        90, 93, 95, 98, 100];
    println!("{}",  arr[scores as usize]);
}

