Радианы и Градусы

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считать два вещественных числа (радианы и градусы соответственно) и вывести (до 7 знаков) для радиан градусы, а для градусов радианы.

radians = degrees * π / 180;
degrees = radians * 180 / π;
 где π=3.1415926535

Вывод и вычисление реализуйте в соответствующих функциях calc_radians(), print_radians(), calc_degrees() и print_degrees() !

Тестовые данные ✅

Sample Input:
1
10

Sample Output:
57.2957795
0.1745329

Напишите программу. Тестируется через stdin → stdout
Верно решили 77 учащихся
Из всех попыток 77% верных



use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {      
    let mut r: f64 = input();
    let mut d: f64 = input(); 
    
    print_degrees(calc_degrees(r));       
    print_radians(calc_radians(d));
}

fn calc_degrees(radians: f64) -> f64 {
    (radians * 180.0) / 3.1415926535
}

fn print_degrees(degrees: f64) {
    println!("{degrees:.7}");
}

fn calc_radians(degrees: f64) -> f64 {
    (degrees * 3.1415926535) /180.0
}

fn print_radians(radians: f64) {
    println!("{radians:.7}");
}