Среднее гармоническое

В редакторе кода представлена неполная программа, которую необходимо доделать. Программа должна считывать натуральные (u32) числа до тех пор пока не будет введено 0, а затем вывести (до 3 знаков) для них среднюю гармоническую.

Среднее гармоническое - один из способов, которым можно понимать "среднюю" величину некоторого набора чисел. Его можно определить следующим образом: пусть даны положительные числа x1, … ,xn, тогда их средним гармоническим будет такое число H, что

n /H = 1 / x1 + ⋯ + 1 / xn.

Можно получить явную формулу для среднего гармонического:

H(x1, … ,xn) = n / ∑_i=1_n 1/xi


Вывод реализуйте в функции print_harm_mean() !

Тестовые данные ✅

Sample Input:
1
2
3
0

Sample Output:
1.636

Напишите программу. Тестируется через stdin → stdout
Верно решили 78 учащихся
Из всех попыток 98% верных


use std::io;
use std::str::FromStr;

fn input<T: FromStr>() -> T 
where
    T::Err: std::fmt::Debug,
{
    let mut input = String::new();
    std::io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input");
    input.trim().parse::<T>().expect("Failed to parse input")
}

fn main() {
    let mut n: u32 = 0;
    let mut sum: f64 = 0.0;       
  
    loop
    {       
        let mut x: u32 = input();
        if x == 0 { break; }        
        n += 1;         
        sum += 1.0 / (x as f64);       
    }  
    
    print_harm_mean((n as f64)/sum);
}

fn print_harm_mean(harm_mean: f64) {
    println!("{harm_mean:.3}");
}


