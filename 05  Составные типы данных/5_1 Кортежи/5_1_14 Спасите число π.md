Спасите число π

Ученик, который плохо учится, решил разделить число Пи на части и поместить каждую часть в отдельный элемент кортежа. Теперь все формулы, которые зависят от числа Пи, находятся в опасности! Допишите программу и выведите (до 7 знаков) получившееся число путем сложения всех элементов кортежа.

Тестовые данные ✅

Sample Input:

Sample Output:
3.1415926

Напишите программу. Тестируется через stdin → stdout
Верно решили 265 учащихся
Из всех попыток 78% верных


fn main() {
    let tup = (3, 0.1, 0.04, 0.001, 0.0005, 0.00009, 0.000002, 0.0000006);
    let (num1, num2, num3, num4, num5, num6, num7, num8) = tup;
    println!("{:.7}", num1 as f64 + num2 + num3 + num4 + num5 + num6 + num7 + num8);
}