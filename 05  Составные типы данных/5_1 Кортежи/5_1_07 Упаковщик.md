Упаковщик

Напишите программу, которая считывает пять строк, сохраняет их в той же последовательности в кортеж и затем выводит получившуюся коллекцию с помощью спецификатора формата :?.

Тестовые данные ✅

Sample Input:
Упаковка
строк
в
кортеж
!

Sample Output:
("Упаковка\n", "строк\n", "в\n", "кортеж\n", "!\n")

Напишите программу. Тестируется через stdin → stdout
Верно решили 282 учащихся
Из всех попыток 80% верных


use std::io;

fn main() {
    let mut smile1 = String::new();
    let mut smile2 = String::new();
    let mut smile3 = String::new();
    let mut smile4 = String::new();
    let mut smile5 = String::new();
    let mut tup: (String, String, String, String, String); 
    
    io::stdin().read_line(&mut smile1).expect("biba");
    io::stdin().read_line(&mut smile2).expect("biba");
    io::stdin().read_line(&mut smile3).expect("biba");
    io::stdin().read_line(&mut smile4).expect("biba");
    io::stdin().read_line(&mut smile5).expect("biba");
    let tup = (smile1, smile2, smile3, smile4, smile5);  
    println!("{:?}", tup); 
}
