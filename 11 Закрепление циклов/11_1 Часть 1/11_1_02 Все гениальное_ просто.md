Все гениальное - просто 💡

Напишите программу, которая считывает натуральное число (u8) и выводит, является ли оно простым или нет. Если является, вывести Число {} является простым, иначе Число {} не является простым.

Простое число - это любое целое число больше 1, у которого нет делителя, кроме 1 и самого себя.

Тестовые данные ✅

Sample Input 1:
2

Sample Output 1:
Число 2 является простым


Sample Input 2:
4

Sample Output 2:
Число 4 не является простым

Напишите программу. Тестируется через stdin → stdout
Верно решили 37 учащихся
Из всех попыток 64% верных


fn is_prime(number: u32) -> bool {
    /*
    Проверяет, является ли число простым.

    Args:
        number: Натуральное число.

    Returns:
        True, если число простое, False - в противном случае.
    */
    if number <= 1 {
        return false; // 1 и меньше не являются простыми
    }
    for i in 2..=((number as f64).sqrt() as u32) {
        if number % i == 0 {
            return false; // Найден делитель, число не простое
        }
    }
    return true; // Прошли все проверки, число простое
}

use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).expect("Ошибка чтения строки");
    let num: u32 = input.trim().parse().expect("Введите натуральное число");
    if is_prime(num) {
        println!("Число {} является простым", num);
    } else {
        println!("Число {} не является простым", num);
    }
}

